# Milestone 009.3 — Read-Only Browser Usability Refinement

Prompt file:

docs/009.3_read_only_browser_usability_refinement_prompt.md

Required closeout file:

docs/009.3_read_only_browser_usability_refinement_closeout.md

## Milestone Mode

Implementation milestone.

Reasoning level: High for initial targeted inspection and UX/data-boundary decisions.
Implementation should then be the smallest safe change consistent with the approved 009.2 reconnaissance.

# Goal

Improve the Repo Control Plane browser so Product Owner, Architect handoff preparation, and future Coder workflow are understandable in human development terms rather than primarily through machine artifact IDs.

This milestone is intentionally limited to:

    read-only browser usability
    presentation derived from existing deterministic facts
    bounded read-only view-model support where needed

It must NOT introduce:

    new persistent metadata models
    new Git mutation authority
    remote fetch
    push
    repository switching
    task/milestone lineage models
    Architect Packet export
    service deployment changes

# Controlling Reconnaissance

Treat the approved closeout as controlling architecture input:

    docs/009.2_product_owner_review_reconciliation_recon_closeout.md

Also use the current implementation and tests as authority.

Do not repeat broad reconnaissance already completed in 009.2.

Inspect only the targeted implementation needed for this milestone.

# Repositories

Repo Control Plane:

    /home/chuck/projects/repo-control

Authorized read-only browser integration fixture:

    /home/chuck/ai-agent-tests/vocab-app

Expected Vocab fixture state:

    branch:
        agent-test/repoctl-ui-demo
    
    HEAD:
        5a251f14a7a69ee2dfc9fc72312f49b0c001c30e
    
    intentional working-tree state:
         M vocab_utils.py

The pending Vocab change is intentional and must remain untouched.

Photo Organizer remains out of scope.

Do not access Photo Organizer.

# Required Preflight

Before implementation, report:

Repo Control:

    git branch --show-current
    git rev-parse HEAD
    git status --short
    git rev-parse @{upstream}
    git rev-list --left-right --count HEAD...@{upstream}

Confirm:

    expected branch
    clean working tree
    HEAD/upstream aligned unless explicitly explained
    009.2 closeout committed

Vocab fixture, read-only:

    git branch --show-current
    git rev-parse HEAD
    git status --short

Expected literal Vocab status:

     M vocab_utils.py

If Repo Control is unexpectedly dirty:

    STOP AND REPORT

If the Vocab fixture differs unexpectedly:

    STOP AND REPORT

Do not clean, reset, restore, stage, commit, or otherwise repair unexplained state.

# Core Product Direction

The browser should increasingly communicate this:

    What repository am I looking at?
    What exact code state is this?
    What changed?
    What evidence exists?
    What deserves review?
    What workflow action happened?

rather than primarily this:

    snap--...
    cmp--...
    ana--...
    stage-plan--...
    commit-exec--...

Machine identifiers remain important immutable audit evidence.

They must remain available.

They simply become secondary to human-readable development information.

# Scope A — Browser Layout and General Readability

Refine the existing browser shell where necessary so desktop space is used intelligently.

Requirements:

- reduce unnecessary horizontal whitespace where current pages remain cramped;
- allow cards/tables containing repository facts to use available desktop width;
- preserve bounded readable width for long prose;
- preserve responsive behavior;
- improve wrapping of:
    branch names
    paths
    SHAs
    artifact IDs
    long workflow text;
- do not create horizontal overflow as the normal desktop experience.

Do not perform an unrelated visual redesign.

Preserve the existing restrained Repo Control visual identity.

# Scope B — Human-Readable Identifier Presentation

Across applicable browser pages:

    Dashboard
    Context
    Snapshots
    Comparisons
    AI Review
    Workflow

change presentation so human-useful facts lead and machine IDs are secondary.

Examples:

Instead of primarily:

    snap--b6616211eed99ee8

prefer a derived presentation such as:

    main @ 5a251f1 — clean snapshot
    Snapshot ID: snap--b6616211eed99ee8

Instead of primarily:

    cmp--f2c6f25db21d630b

prefer a derived presentation such as:

    5a251f1 unstaged state → 5a251f1 staged state
    Comparison ID: cmp--f2c6f25db21d630b

Exact wording may follow the facts available in current artifacts.

Requirements:

- use only existing deterministic facts;
- do not invent milestone names or semantic purpose labels;
- do not persist new user-authored labels;
- short SHA may be primary;
- full SHA must remain available in secondary detail where meaningful;
- immutable artifact ID must remain visible or readily available.

If a meaningful human label cannot be safely derived:

    preserve the machine ID

Do not manufacture a misleading description.

# Scope C — Dashboard Git State

Improve Git State readability.

Current deterministic facts remain authoritative.

Present clearly:

    Repository
    Branch
    HEAD
    Workflow state
    Active Git operation
    Upstream information already locally known

Desired improvements:

- short HEAD SHA as primary;
- full SHA available in secondary detail;
- human-readable commit subject if already safely available from local Git without network access;
- Git tags may be shown if already locally available and simple to derive read-only;
- improve wrapping;
- provide concise explanatory help for:
    workflow states
    active Git operation
    locally-known upstream caveat.

Workflow state help should make understandable:

    clean
    staged_only
    unstaged_only
    staged_and_unstaged
    conflicted

Do not fetch.

Do not contact a remote.

Do not imply upstream information is current beyond locally-known refs.

# Scope D — Working Tree File Details

The Dashboard currently reports category counts.

Add a read-only detail affordance showing the files represented by:

    Staged
    Unstaged
    Untracked
    Conflicts

Use the exact path/category facts already produced by Repo Control Git-state logic.

Requirements:

- no Git mutation;
- preserve exact classification;
- clearly associate paths with their category;
- handle zero-count categories cleanly;
- handle long paths;
- do not recompute classification independently in the template when core status data already provides it.

This view should become a useful precursor to future M010 Stage review, but M009.3 must not add staging controls.

# Scope E — Context Human Readability

Improve the Deterministic Context page without changing its deterministic matching semantics.

Preserve:

    deterministic selection
    bounded result size
    lexical/relation reasons
    existing artifact identity
    related files
    symbols
    relationships
    tests

## Context Header

Where existing context data supports it, show prominently:

    Repository
    Branch
    HEAD at generation
    Working-tree state at generation
    Query
    Match status
    Seed count

Machine context ID becomes secondary.

## CURRENT / STALE Cue

The existing context artifact contains repository/branch/head/working-tree origin facts.

Where safely derivable using existing current read-only repository state, show a presentation-level cue such as:

    CURRENT

or:

    STALE / repository state changed since context generation

This is informational only.

Do NOT implement the later strict task/coder freshness contract.

Do NOT create a new persistent binding model.

Do NOT claim a context is safe for coder execution merely because this presentation check says CURRENT.

If current-vs-origin comparison cannot be made safely from existing facts:

    show UNKNOWN rather than infer.

## Why Included

Retain why each file was selected, such as:

    lexical match
    relationship expansion
    related test

Do not remove evidence provenance for the sake of visual simplicity.

# Scope F — Context Test-Evidence Aggregation

The 009.2 reconnaissance established that apparent duplicate Related Tests are not deterministic evidence corruption.

For preserved context:

    synonym--ee6b5af5c217

multiple distinct deterministic rows render as identical-looking browser rows because distinguishing fields such as:

    source_line
    test_info.reference_kind

are not shown.

The safe 009.3 rule is:

    preserve deterministic records
    aggregate primary browser presentation
    expose underlying evidence details secondarily

Implement browser presentation so the primary Related Tests list groups by the visible logical test-target identity, for example:

    test_file
    test_name
    target_file
    target_symbol

A grouped entry may show useful information such as:

    test_ordering_independent
        target: normalize_synonym_candidates
        evidence references: 6

Secondary expandable/detail presentation may show:

    reference kind
    source line
    class/kind metadata
    other existing distinguishing deterministic fields

Requirements:

- do not delete or rewrite deterministic records;
- do not alter context artifact generation;
- do not alter matching semantics;
- do not collapse records that are genuinely different without retaining access to their distinguishing evidence;
- genuinely exact duplicate deterministic identities may be visually deduplicated if proven identical.

Also clarify the difference between:

    Direct Relationships

and:

    Related Tests

so cross-section evidence does not look like accidental duplication.

# Scope G — Snapshots

Improve snapshot presentation using existing immutable snapshot facts.

Primary presentation should emphasize available human-useful facts such as:

    branch
    short HEAD
    working-tree state
    clean/dirty status
    matches-current status

Machine snapshot ID remains secondary.

Do NOT add:

    persisted purpose labels
    editable snapshot names
    milestone names
    code-lineage records
    automatic snapshot creation

The user should better understand:

    what code state this snapshot represents

without implying:

    why the snapshot was created

unless that purpose already exists as authoritative stored data.

# Scope H — Comparisons

Improve Before/After selection and comparison presentation.

Use human-readable derived snapshot descriptions in selectors and result views.

Example concept:

    Before:
        5a251f1 — clean
    
    After:
        5a251f1 — unstaged changes

with snapshot IDs secondary.

Requirements:

- keep exact Before and After snapshot IDs available;
- preserve deterministic comparison behavior;
- do not implement a new strict lineage/order model;
- do not reject comparisons based on assumptions that cannot currently be proven;
- where current evidence can safely communicate state difference, use it;
- otherwise remain neutral.

Do not solve the later generalized Before→After ordering problem in this milestone.

# Scope I — AI Review

Preserve the accepted AI authority boundary:

    deterministic Repo Control evidence = authoritative
    GPT-OSS analysis = advisory

Improve presentation so the review is easier to interpret.

Primary presentation should emphasize:

    compared states
    Summary
    Review Signals
    Questions for Human Review

Machine analysis/comparison IDs should be secondary.

Retain:

    evidence references
    severity
    signal category
    advisory disclaimer

Where useful, make evidence IDs available in secondary detail rather than visually dominating the page.

Do not change:

    local model authority
    analysis generation semantics
    deterministic comparison authority
    evidence grounding contract

# Scope J — Workflow History

Improve the Workflow page so it reads as a development audit trail rather than primarily a list of machine objects.

For each plan/execution, emphasize available facts such as:

    Action
    Branch
    Starting HEAD
    File/count scope
    Compatibility
    Execution result/status

Machine IDs remain available as secondary audit evidence.

Make distinctions understandable between:

    prepare-stage
    stage execution
    prepare-commit
    commit execution

and between:

    compatible
    stale/incompatible
    executed
    not executed

Do not add browser Stage or Commit controls.

Those belong to M010.

# Explicitly Out of Scope — Whitespace

Although 009.2 identified whitespace diagnostics as a possible read-only extension:

    DO NOT implement whitespace diagnostics in 009.3.

Do not add:

    git diff --check UI
    whitespace-only diff filtering
    whitespace normalization
    formatting cleanup controls

Whitespace work remains deferred for a later bounded milestone.

This keeps 009.3 focused on the existing browser evidence model.

# Explicitly Deferred Architecture

Do NOT implement any of the following:

- repository registry;
- repository chooser/switcher;
- arbitrary path entry;
- persistent snapshot purpose labels;
- automatic workflow-boundary snapshots;
- code commit -> code commit lineage model;
- task model;
- strict coder-context freshness contract;
- Milestone Record;
- Architect Packet/export;
- source excerpt export;
- upstream fetch;
- background remote refresh;
- guarded Push;
- browser Stage;
- browser Commit;
- whitespace normalization;
- service/systemd startup;
- SSH tunnel automation;
- Photo Organizer integration.

# M010 Boundary Protection

M010 remains:

    Browser Guarded Stage + Commit

using existing M008/M007 authorities.

009.3 may improve browser presentation in ways that make future M010 review clearer.

009.3 must NOT:

- add Stage buttons;
- add Commit buttons;
- create approval routes;
- call stage/commit mutation services;
- alter M007/M008 safety contracts;
- create a generalized browser mutation framework.

If implementation reveals that a requested 009.3 presentation change requires changing Stage/Commit authority:

    STOP AND REPORT

# Implementation Guidance

Prefer:

    existing core data
        ↓
    thin view-model transformation
        ↓
    template presentation

Do not duplicate repository truth logic inside templates.

Do not introduce a parallel Git-status implementation if existing workflow/status services already provide the fact.

Do not change deterministic artifact schemas solely to make the UI prettier unless a missing field is proven necessary and the change remains bounded/read-only.

If a requested presentation requires new persistence or materially changes artifact identity:

    STOP AND REPORT

Do not broaden repository reconnaissance.

Inspect only named or directly implicated files/services from 009.2, likely including:

    src/repoctl/web/static/app.css
    src/repoctl/web/views.py
    src/repoctl/web/app.py
    src/repoctl/web/templates/base.html
    src/repoctl/web/templates/dashboard.html
    src/repoctl/web/templates/context.html
    src/repoctl/web/templates/snapshots.html
    src/repoctl/web/templates/comparisons.html
    src/repoctl/web/templates/analysis.html
    src/repoctl/web/templates/workflow.html

and only supporting read-only services required by implementation, such as:

    src/repoctl/workflow/status.py
    src/repoctl/workflow/git_state.py
    src/repoctl/context/generator.py
    src/repoctl/snapshot/manager.py
    src/repoctl/compare/manager.py

Likely primary validation surface:

    tests/test_web.py

Inspect other files only when a concrete dependency requires them.

# Browser Safety Requirements

Preserve all M009/M009.1 browser safety properties:

- loopback-only host enforcement;
- bound repository model;
- no arbitrary filesystem repository input;
- CSRF protections;
- POST/Redirect/GET behavior where applicable;
- escaped user/repository content;
- no browser Git mutation;
- no push/fetch/pull;
- no CDN/Node dependency introduction;
- existing local Flask/Jinja architecture.

# Validation

At minimum:

1. targeted automated browser tests covering changed behavior;
2. full Repo Control test suite;
3. browser startup against the Vocab fixture;
4. Product Owner-visible smoke inspection of all materially changed pages.

Required browser smoke pages:

    Dashboard
    Context
    Snapshots
    Comparisons
    AI Review
    Workflow

Use the already-existing Vocab evidence where possible.

Do not create unnecessary new Vocab artifacts merely to populate pages.

Do not modify the Vocab fixture.

For the known context:

    synonym--ee6b5af5c217

verify specifically:

- logical Related Tests no longer appear as misleading unexplained repetitions;
- grouped presentation retains access to distinct underlying evidence;
- deterministic stored context remains unchanged.

# Regression Requirements

Confirm:

- CLI behavior unchanged unless a bounded read-only helper was required;
- M007 guarded Commit tests remain passing;
- M008 guarded Stage tests remain passing;
- M009/M009.1 browser safety behavior remains passing;
- deterministic context artifacts are not rewritten;
- snapshot/comparison/analysis/workflow artifacts are not rewritten;
- no Git mutation is available from the browser.

# Escalation Protocol

STOP and report before implementation or at the first affected boundary if any of the following is discovered:

1. Repo Control starting tree is unexpectedly dirty.

2. Vocab fixture differs from the expected preserved state.

3. Human-readable snapshot/comparison labels require new persistent metadata rather than derivation from existing facts.

4. CURRENT/STALE Context presentation requires a new task-binding model rather than a bounded read-only comparison.

5. Context test grouping cannot preserve deterministic evidence identity.

6. Working Tree details require a second independent Git parser rather than using existing status data.

7. AI presentation requires changing analysis authority or evidence semantics.

8. Workflow presentation requires modifying M007/M008 mutation contracts.

9. Any requested feature requires:
   
       fetch
       push
       stage
       commit
       reset
       restore
       clean
       new repository persistence
       service installation

10. Photo Organizer access would be required.

Do not improvise around a locked boundary.

# Required Closeout

Create:

docs/009.3_read_only_browser_usability_refinement_closeout.md

Include:

1. status;
2. exact starting Repo Control branch/HEAD/upstream/status;
3. exact starting Vocab branch/HEAD/status;
4. files changed;
5. implementation summary;
6. general layout/readability changes;
7. identifier-presentation changes;
8. Dashboard Git State changes;
9. Working Tree detail implementation;
10. Context header/current-stale presentation;
11. Context test-evidence grouping implementation;
12. confirmation deterministic Context artifacts were not changed;
13. Snapshot presentation changes;
14. Comparison presentation changes;
15. AI Review presentation changes;
16. Workflow presentation changes;
17. explicit confirmation whitespace diagnostics were not implemented;
18. explicit confirmation no browser Stage/Commit controls were added;
19. explicit confirmation no fetch/push capability was added;
20. explicit confirmation no persistent metadata/model was added;
21. targeted tests and literal results;
22. full Repo Control test result;
23. browser/Vocab smoke result;
24. Product Owner-visible pages requiring review;
25. Vocab preservation confirmation;
26. Photo Organizer non-access confirmation;
27. known limitations/deferred items;
28. exact final Repo Control `git status --short`;
29. exact final Vocab `git status --short`.

# Definition of Done

009.3 is complete only when:

- the browser is materially easier for a human Product Owner to understand;
- existing deterministic repository facts are presented before opaque machine identifiers;
- exact IDs remain available for audit;
- Working Tree file details are available read-only;
- Context evidence is clearer without changing deterministic context semantics;
- apparently duplicate Related Tests are safely grouped while underlying evidence remains preserved;
- Snapshot, Comparison, AI Review, and Workflow pages communicate development meaning more clearly;
- no new persistent model exists;
- no browser Git mutation exists;
- no remote Git operation exists;
- Vocab remains unchanged;
- Photo Organizer remains untouched;
- tests pass;
- M010 remains a clean, separately bounded Browser Stage + Commit milestone.
