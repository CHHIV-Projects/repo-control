# Milestone 005.1 — Ollama GPT-OSS Structured Output Compatibility

Prompt file:

`005.1_ollama_gpt_oss_structured_output_compatibility_prompt.md`

Required closeout file:

`005.1_ollama_gpt_oss_structured_output_compatibility_closeout.md`

## Objective

Resolve the bounded live-provider escalation from Milestone 005.

Milestone 005 implementation is complete and its deterministic/safety architecture is accepted, but live acceptance failed because every bounded GPT-OSS invocation ended with:

    provider request failed:
    ollama structured response is not valid JSON

Both required live scenarios failed:

- controlled non-zero structural comparison: 3/3 attempts;
- zero-delta structural comparison: 3/3 attempts.

Invalid responses were correctly rejected and no analysis artifacts were published.

Milestone 005.1 must determine exactly where the incompatibility occurs and implement the smallest safe correction required for the existing Milestone 005 design to pass live validation.

This is a remediation milestone.

Do not add new product capability.

The target outcome is:

    existing Milestone 005 architecture
        +
    corrected Ollama/GPT-OSS compatibility
        ->
    successful grounded live analysis

Do not weaken evidence grounding, response validation, zero-delta protection, local-only operation, or immutable publication rules merely to obtain a passing response.

---

# Controlling Documents

Read:

- `docs/Repo_Control_Plane_v0.1_Architecture_and_Implementation_Plan.md`
- `docs/005_gpt_oss_structural_delta_analysis_prompt.md`
- `docs/005_gpt_oss_structural_delta_analysis_closeout.md`

Also preserve the trusted behavior established by Milestones 001–004.

Milestone 005 closeout is the controlling failure evidence for this remediation.

Do not reopen unrelated architecture.

---

# Project

Repo Control Plane:

`/home/chuck/projects/repo-control`

Local Ollama endpoint:

    http://127.0.0.1:11434

Required model:

    gpt-oss:20b

Photo Organizer remains explicitly out of scope.

Do not inspect, scan, snapshot, compare, analyze, modify, benchmark, or otherwise access Photo Organizer.

---

# Git Preflight

Before coding:

1. confirm Repo Control Plane worktree is completely clean;
2. confirm current branch and HEAD;
3. confirm upstream state if configured;
4. stop and report unexpected pre-existing changes.

Do not automatically reset, clean, restore, stage, commit, or push.

Do not commit or push unless separately instructed.

---

# Scope

Milestone 005.1 may modify only what is necessary to diagnose and repair the existing local Ollama/GPT-OSS provider path.

Likely scope:

- `src/repoctl/analysis/provider.py`
- narrowly related analysis contracts/tests;
- README only if provider behavior documentation must change;
- CLI only if a small error-reporting integration change is necessary.

Do not redesign:

- analysis packet construction;
- deterministic evidence selection;
- evidence IDs;
- response semantic schema;
- immutable analysis storage;
- snapshots;
- comparisons;
- context generation;
- scanner behavior.

---

# Key Compatibility Finding to Verify

The committed Milestone 005 provider used:

    think = false

The current native Ollama contract for GPT-OSS requires a thinking level rather than boolean disablement.

For GPT-OSS, test the native API using:

    think = "low"

Milestone 005.1 must NOT simply assume this is the complete root cause.

First verify the actual failure boundary.

The intended production behavior after remediation, if supported by live evidence, is:

    model = "gpt-oss:20b"
    stream = false
    format = <JSON schema object>
    options.temperature = 0
    think = "low"
    tools = absent

GPT-OSS thinking output is not part of Repo Control Plane analysis evidence.

If Ollama returns:

    message.thinking

Repo Control Plane must:

- ignore it;
- never persist it;
- never place it in `analysis_input.json`;
- never place it in `analysis.json`;
- never place it in `analysis.md`;
- never expose it as evidence;
- never include it in provider failure diagnostics.

Only:

    message.content

is eligible to become the structured advisory model output.

---

# Gate A — Reproduce and Classify the Existing Failure

Before changing production provider behavior, inspect the current implementation and reproduce the failure sufficiently to classify it.

Determine whether:

    "ollama structured response is not valid JSON"

means:

1. HTTP response body itself is not valid JSON;
2. Ollama response envelope is valid JSON but `message` is malformed/missing;
3. `message.content` is absent or not a string;
4. `message.content` is a string but is not valid JSON;
5. `message.content` is valid JSON but fails Repo Control Plane response-schema validation;
6. another precisely identifiable provider-contract failure.

Do not use one generic "not valid JSON" error for all these cases after Milestone 005.1.

Production errors should distinguish at minimum:

    invalid_ollama_envelope
    missing_message_content
    invalid_structured_content_json
    structured_content_validation_failed

Equivalent narrowly named internal values are acceptable.

Do not include hidden thinking content in error messages.

Do not persist raw provider output merely for debugging.

---

# Gate B — Minimal Native Ollama Compatibility Probe

Using the exact installed:

    gpt-oss:20b

perform a minimal local API probe against:

    POST http://127.0.0.1:11434/api/chat

Use:

    stream = false
    think = "low"
    temperature = 0

and a very small JSON schema supplied through:

    format

For example, conceptually require an object with one short string field.

The exact diagnostic schema wording is an implementation choice.

Verify separately:

1. HTTP request succeeds;
2. outer Ollama response is valid JSON;
3. response contains `message.content`;
4. `message.content` is valid JSON;
5. returned JSON conforms to the requested minimal schema.

Also note whether `message.thinking` is present.

Do not store or reproduce its contents.

If the minimal official-style structured-output probe fails:

    STOP — ESCALATION REQUIRED

Report:

- HTTP status;
- content type;
- which parsing/validation stage failed;
- Ollama/model identity;
- model digest;
- confirmation that no reasoning content was retained.

Do not weaken Milestone 005 or switch APIs merely to continue.

---

# Gate C — Existing Production Schema Probe

If Gate B succeeds, exercise the actual Milestone 005 structured-output schema through the same native Ollama endpoint before changing broader application behavior.

Use a small controlled evidence packet if practical.

Verify:

- outer Ollama JSON envelope;
- `message.content`;
- content JSON validity;
- response semantic schema;
- evidence-ID shape;
- string restrictions;
- required fields.

The purpose is to distinguish:

    transport/model thinking incompatibility

from:

    production JSON-schema incompatibility

from:

    application parser incompatibility.

If the minimal schema succeeds but the production schema fails, identify the exact incompatibility before changing code.

Do not arbitrarily simplify the production semantic contract.

---

# Production Provider Correction

If Gates A–C establish that native Ollama structured output works with the compatible GPT-OSS request contract, make the smallest production change.

Expected correction unless evidence proves otherwise:

    think = "low"

instead of:

    think = false

Preserve:

    model = gpt-oss:20b
    stream = false
    structured schema through format
    temperature = 0
    no tools
    one request per invocation
    local loopback only

Do not introduce:

- automatic retries;
- alternate models;
- another provider;
- `/api/generate`;
- OpenAI-compatible endpoint fallback;
- `format = "json"` fallback;
- prompt-only JSON enforcement;
- cloud fallback.

Those require a separate decision if native schema-constrained `/api/chat` cannot satisfy the milestone.

---

# Ollama Envelope Parsing Contract

The provider must treat the native Ollama response as two separate structured layers.

## Layer 1 — Ollama Envelope

The HTTP response body must first parse as JSON.

Require the expected response envelope sufficiently to locate:

    message.content

Do not interpret the entire Ollama response envelope as the GPT-OSS analysis object.

Provider telemetry such as durations or token counts is not analysis evidence.

## Layer 2 — Structured Model Content

Then parse:

    message.content

as the GPT-OSS structured JSON result.

Whitespace surrounding the JSON string may be ignored.

Do NOT:

- extract JSON from Markdown fences;
- search for the first `{`;
- strip arbitrary prose;
- repair malformed JSON;
- ask the model to repair itself;
- silently transform protocol debris into JSON.

If `message.content` is not valid JSON:

    fail closed.

The structured-output API should produce structured content; Repo Control Plane should not build a permissive recovery parser around malformed generation.

---

# Thinking / Reasoning Boundary

GPT-OSS may require a non-disabled thinking mode at the Ollama protocol level.

Use the lowest supported setting:

    think = "low"

Repo Control Plane does not consume that reasoning.

Any returned:

    message.thinking

must be treated as transient provider data and discarded.

Do not:

- persist it;
- log it;
- hash it into analysis identity;
- render it;
- expose it in debugging output;
- use it to validate the answer;
- treat it as evidence.

The only model-generated data eligible for analysis publication is the validated structured object obtained from:

    message.content.

The existing prohibition against storing chain-of-thought remains controlling.

---

# Provider Diagnostic Improvements

Improve provider failure classification enough that a future escalation can distinguish:

    network/HTTP failure
    outer JSON-envelope failure
    missing/invalid message.content
    inner structured JSON failure
    Repo Control Plane semantic-schema failure
    evidence-grounding failure

Keep diagnostics concise.

Do not add a general verbose tracing system.

Do not persist raw prompts/responses as new artifacts.

Do not expose hidden reasoning.

---

# Response Contract Remains Locked

Do not weaken the Milestone 005 response contract.

The required semantic fields remain:

    summary
    summary_evidence_ids
    review_signals
    questions_for_human_review

All existing validation remains required, including:

- exact allowed fields;
- enums;
- array limits;
- string limits;
- no embedded CR/LF where already specified;
- evidence-ID existence;
- duplicate evidence-ID protection;
- zero-delta rule;
- non-zero-delta minimum signal rule.

Provider compatibility may change.

Grounding semantics may not.

---

# Zero-Delta Rule Remains Locked

For a true zero structural delta:

    review_signals = []
    questions_for_human_review = []

Any live output violating this remains invalid.

Do not relax this rule to accommodate GPT-OSS.

---

# Non-Zero Rule Remains Locked

For authoritative non-zero aggregate delta:

    review_signals

must contain at least one grounded signal.

`A001` remains sufficient evidence for a cautious aggregate-only signal if detailed packet truncation prevents greater specificity.

Do not require the model to manufacture high-priority concerns.

---

# Model Identity Remains Locked

Continue resolving exact model identity through:

    GET /api/tags

Require exactly:

    name == "gpt-oss:20b"

and valid exact digest.

No model substitution.

Record the same provenance required by Milestone 005.

---

# Automated Tests

Add focused regression tests for at least:

1. GPT-OSS production request uses `think="low"`;
2. provider does not use `think=false`;
3. structured JSON schema remains supplied through `format`;
4. `stream=false`;
5. `temperature=0`;
6. no tools;
7. one request per invocation;
8. outer Ollama response JSON parses separately from model content;
9. malformed outer envelope fails with distinct classification;
10. missing `message` fails;
11. missing `message.content` fails;
12. non-string `message.content` fails;
13. invalid JSON inside `message.content` fails distinctly;
14. valid content JSON proceeds to semantic validation;
15. semantic-schema failure remains distinct from JSON parse failure;
16. `message.thinking` may exist but is ignored;
17. thinking content is never included in analysis artifacts;
18. thinking content is never included in error diagnostics;
19. Markdown-fenced JSON is rejected rather than extracted;
20. prose-prefixed/suffixed JSON is rejected;
21. no `format="json"` fallback exists;
22. no alternate endpoint/model/provider fallback exists;
23. zero-delta validation remains strict;
24. non-zero grounding remains strict;
25. all Milestone 001–005 automated tests remain passing.

Use fake provider responses for exact parser/error assertions.

Automated tests must not require live Ollama.

---

# Live Validation Gate 1 — Minimal Structured Output

After tests pass, perform the Gate B minimal native GPT-OSS schema probe.

Record only:

- model name;
- digest;
- request contract;
- pass/fail stage;
- whether outer response parsed;
- whether structured content parsed;
- whether schema validation passed;
- whether thinking field was present as a boolean observation only.

Do not record thinking contents.

---

# Live Validation Gate 2 — Controlled Non-Zero Analysis

Re-use or recreate a disposable fixture comparison with known non-zero structural delta.

Do not modify Vocab App just to generate evidence.

Run:

    repoctl analyze <non_zero_comparison_id> \
        --repository <disposable_fixture>

Allow at most three separate explicit invocations.

Each invocation still performs exactly one model request.

Acceptance requires at least one invocation that:

- receives valid Ollama envelope;
- receives valid structured `message.content`;
- passes Repo Control Plane response schema;
- passes evidence grounding;
- contains at least one review signal;
- cites only valid packet evidence IDs;
- publishes immutable analysis artifacts successfully.

Then inspect the published result.

Verify:

- deterministic evidence supports every cited observation;
- interpretations remain advisory;
- no unsupported repository fact is asserted as fact;
- no source-code body was supplied;
- no thinking output was stored.

---

# Live Validation Gate 3 — Zero-Delta Analysis

Use a valid zero-delta/self-comparison.

Run:

    repoctl analyze <zero_delta_comparison_id> \
        --repository <appropriate_fixture_or_safe_read_only_target>

Allow at most three separate explicit invocations.

Acceptance requires at least one invocation producing:

    review_signals = []
    questions_for_human_review = []

with a valid evidence-grounded summary.

Verify no reasoning/thinking content is stored.

---

# Vocab App

Vocab App may be used only if useful as an unchanged read-only zero-delta validation target.

If used:

- capture exact porcelain-v2 status before/after;
- require exact equality;
- do not modify it;
- do not create files within it.

A disposable fixture is acceptable instead.

---

# Acceptance Outcomes

## PASS

Milestone 005.1 passes only if:

- automated tests pass;
- minimal native structured-output probe succeeds;
- controlled non-zero live analysis succeeds;
- zero-delta live analysis succeeds;
- all existing Milestone 005 grounding safeguards remain intact;
- no chain-of-thought is persisted;
- target repository remains unmodified.

If all pass, closeout should explicitly state:

    MILESTONE 005 ACCEPTANCE: PASS
    MILESTONE 005.1: COMPLETE

This closes the Milestone 005 escalation.

## ESCALATION

If native Ollama + exact `gpt-oss:20b` cannot satisfy the structured-output contract after the bounded remediation and live attempts:

    MILESTONE 005 ACCEPTANCE: ESCALATION REQUIRED
    MILESTONE 005.1: ESCALATION REQUIRED

Report the exact failing layer.

Do NOT automatically respond by:

- weakening JSON validation;
- extracting JSON heuristically;
- switching models;
- switching API families;
- allowing prompt-only JSON;
- enabling cloud AI;
- adding retry loops.

That would require a new Product Owner / Architect decision.

---

# Explicit Non-Goals

Do not implement:

- new AI features;
- source-code access for GPT-OSS;
- source-code editing;
- Aider orchestration;
- Codex/Copilot integration;
- multi-model support;
- model fallback;
- cloud fallback;
- OpenAI-compatible endpoint fallback;
- `/api/generate` fallback;
- automatic retry;
- JSON repair;
- Markdown-fence extraction;
- agent loops;
- tool calls;
- model auto-download;
- chain-of-thought storage;
- verbose provider tracing infrastructure;
- Git writes;
- Docker changes;
- Photo Organizer access.

---

# Implementation Discipline

Use the existing Milestone 005 analysis architecture.

Preferred remediation flow:

    reproduce exact failure
        ↓
    classify outer-envelope vs inner-content failure
        ↓
    verify minimal native Ollama structured output
        ↓
    verify production response schema
        ↓
    make smallest compatibility correction
        ↓
    automated regression validation
        ↓
    controlled non-zero live analysis
        ↓
    zero-delta live analysis

Do not rebuild the provider architecture.

Do not broaden reconnaissance after the specific failure is classified.

If:

    think=false

is confirmed as the incompatibility and:

    think="low"

correctly restores schema-constrained `message.content`, prefer that minimal fix.

If evidence shows a different narrowly bounded parser/request bug, fix that instead and document it precisely.

---

# Validation Before Closeout

Run at minimum:

- full Repo Control Plane automated suite;
- Milestone 005/005.1 focused tests;
- Python syntax/compile validation;
- minimal live structured-output probe;
- controlled non-zero live analysis;
- zero-delta live analysis;
- direct evidence-grounding inspection;
- confirmation no thinking data appears in published artifacts;
- target Git-status equality where applicable;
- `git diff --check`;
- final `git status --short`.

---

# Required Closeout

Create:

`docs/005.1_ollama_gpt_oss_structured_output_compatibility_closeout.md`

Include:

1. status — PASS or ESCALATION;
2. exact original failure classification;
3. root cause established by evidence;
4. files changed;
5. exact provider request before remediation;
6. exact provider request after remediation;
7. minimal structured-output probe result;
8. Ollama model name/digest;
9. handling of `message.thinking`;
10. provider parser/error-classification changes;
11. automated test results;
12. controlled non-zero live result;
13. representative valid review signals/evidence IDs if successful;
14. zero-delta live result;
15. confirmation existing grounding contracts remained unchanged;
16. confirmation no hidden reasoning was stored;
17. target repository before/after Git status;
18. limitations;
19. whether Milestone 005 acceptance is now PASS;
20. recommended next milestone;
21. final Repo Control Plane `git status --short`.

Do not commit or push unless separately instructed.

---

# Final Acceptance Criteria

Milestone 005.1 passes only if:

- the original malformed structured-response failure is precisely classified;
- the root cause is evidenced rather than guessed;
- production GPT-OSS request uses a supported native Ollama contract;
- schema-constrained output remains enabled;
- model thinking data is discarded and never persisted;
- provider parsing cleanly separates Ollama envelope from structured model content;
- malformed output still fails closed;
- no permissive JSON-repair behavior is introduced;
- all automated tests pass;
- controlled non-zero live GPT-OSS analysis succeeds;
- zero-delta live GPT-OSS analysis succeeds;
- every accepted AI observation remains grounded in deterministic evidence IDs;
- target repositories remain unmodified;
- Milestone 005 can be formally moved from ESCALATION REQUIRED to ACCEPTED.




# Product Owner / Architect Clarification Addendum — Milestone 005.1 Execution and Diagnostic Contract

This addendum resolves the final pre-coding questions for Milestone 005.1.

It does not broaden the remediation scope.

## 1. Clean Preflight Is Literal

Milestone 005 implementation and its escalation closeout were committed before Milestone 005.1 began.

Therefore the initial Milestone 005.1 preflight requires a completely clean Repo Control Plane worktree.

If:

    git status --short

or equivalent porcelain-v2 inspection reports any tracked or untracked entries before 005.1 implementation begins:

- stop;
- report the exact entries;
- report current branch and HEAD;
- do not assume they are expected Milestone 005 continuation work;
- do not reset, clean, restore, stage, commit, or discard them automatically.

There is no approved dirty baseline for the start of Milestone 005.1.

If the coder currently believes uncommitted Milestone 005 changes are present, re-check repository state rather than proceeding on that assumption.

## 2. Stable Provider Failure Codes

Provider/response failure classification must use stable machine-readable internal codes.

These codes must also be surfaced in concise CLI failure output so operator evidence and closeout evidence can identify the exact failing layer.

Use at minimum:

    provider_http_failure
    invalid_ollama_envelope
    missing_message
    missing_message_content
    invalid_message_content_type
    invalid_structured_content_json
    structured_content_validation_failed
    evidence_grounding_validation_failed

Equivalent names are acceptable only if equally narrow and documented in the closeout.

The CLI should expose the code plus a concise human-readable message, conceptually:

    provider error [invalid_structured_content_json]:
    message.content was not valid JSON

Do not include:

- raw full provider envelopes;
- raw `message.thinking`;
- chain-of-thought;
- arbitrary generated content beyond the minimum safe diagnostic necessary.

These codes are diagnostic classification, not a new public API stability promise beyond Milestone 005.1.

## 3. Gate B and Gate C Are Validation Procedures, Not New Commands

Do not add a new user-facing command or checked-in standalone diagnostic utility merely to perform Gate B or Gate C.

Preferred approach:

- use the existing provider implementation or narrowly factored internal helper functions;
- use direct local one-off validation invocation where necessary;
- cover deterministic parsing/request behavior with automated fake-provider tests.

A small internal helper used by production code and tests is acceptable.

Do not add:

    repoctl probe-ollama
    repoctl debug-provider

or a permanent standalone diagnostic script in Milestone 005.1.

If a temporary local command/script is used during investigation, it must not become part of the committed product unless separately approved.

## 4. Live Probe Evidence

Automated tests must capture all deterministic contracts that can be tested without live Ollama, including:

- request shape;
- thinking-level value;
- envelope parsing;
- structured-content parsing;
- error classification;
- schema validation;
- grounding behavior;
- thinking-field discard behavior.

The closeout must capture summarized live evidence for Gates B and C.

For each live probe report only what is necessary, such as:

- model name;
- model digest;
- endpoint;
- request contract fields relevant to compatibility;
- HTTP success/failure;
- outer-envelope parse pass/fail;
- `message.content` presence/type pass/fail;
- structured-content JSON pass/fail;
- semantic-schema validation pass/fail;
- whether `message.thinking` was present as a boolean observation only;
- resulting stable failure code, if any.

Do not record or persist:

- `message.thinking` contents;
- raw chain-of-thought;
- complete raw response envelopes merely for debugging.

The closeout is the durable live-validation record.

Do not create new runtime diagnostic artifacts for these probes.

## 5. Gate B Pass / Gate C Fail Boundary

If the minimal Gate B structured-output probe succeeds but the actual Milestone 005 production-schema Gate C probe fails, Milestone 005.1 may continue only with the smallest compatibility correction that preserves the existing semantic contract.

Permitted examples include:

- correcting provider request construction;
- correcting native Ollama envelope parsing;
- correcting how the existing JSON schema is serialized/supplied;
- reformulating the provider-facing JSON Schema representation when the resulting accepted response contract is semantically equivalent;
- correcting an application parser mismatch.

The existing Repo Control Plane semantic validator remains authoritative.

A provider-facing schema representation change is acceptable only when it preserves the same required semantics, including:

- required top-level fields;
- field types;
- allowed enums;
- array limits;
- text limits;
- evidence-ID requirements;
- zero-delta rule;
- non-zero-delta grounding rule.

If Gate C appears to require weakening any of those semantics:

    STOP — ESCALATION REQUIRED

Do not:

- make required fields optional;
- broaden enums merely for model convenience;
- remove grounding requirements;
- weaken evidence-ID validation;
- weaken zero-delta behavior;
- accept malformed JSON;
- switch to prompt-only JSON enforcement.

The compatibility layer may change.

The Milestone 005 grounding contract may not.

## 6. Error Layer Classification

Gate A must classify the original failure at the narrowest established layer.

The diagnostic progression should conceptually be:

    HTTP/network
        ↓
    Ollama outer JSON envelope
        ↓
    message object
        ↓
    message.content presence/type
        ↓
    message.content JSON syntax
        ↓
    Repo Control Plane semantic schema
        ↓
    evidence grounding

Stop classification at the first failed layer.

Do not report:

    "structured response is invalid"

when a narrower established classification is available.

This error taxonomy must be shared by live behavior and fake-provider regression tests where applicable.

## 7. Thinking Safety Applies to Diagnostics Too

No diagnostic path may serialize or log an entire Ollama `message` object if it could contain:

    message.thinking

Provider error construction must extract only the explicitly approved safe fields needed for classification.

The presence of thinking data may be represented only as:

    thinking_present = true

or:

    thinking_present = false

during bounded live validation evidence.

Never include its content.

This applies equally to:

- CLI errors;
- exceptions;
- test failure messages;
- closeout evidence;
- analysis artifacts.

## 8. No New Diagnostic Product Surface

Milestone 005.1 is remediation of the existing:

    repoctl analyze

provider path.

It does not authorize a permanent provider-debug subsystem.

After remediation, normal operators should still use:

    repoctl analyze <comparison_id> [--repository <path>]

The improved failure codes may make that command more diagnosable, but no separate provider-management workflow should be introduced.

## 9. Acceptance Boundary

The original binary acceptance rule remains controlling.

Milestone 005.1 is not complete merely because:

- the provider failure is better classified;
- `think="low"` is implemented;
- Gate B succeeds;
- automated tests pass.

Full acceptance still requires:

1. automated suite passes;
2. minimal Gate B native structured-output probe passes;
3. production-schema Gate C compatibility is established;
4. at least one controlled non-zero live `repoctl analyze` invocation succeeds;
5. at least one zero-delta live `repoctl analyze` invocation succeeds;
6. existing grounding safeguards remain intact;
7. no thinking content is retained;
8. target repositories remain unmodified.

If any required live gate remains unsatisfied after the bounded attempts:

    MILESTONE 005 ACCEPTANCE: ESCALATION REQUIRED
    MILESTONE 005.1: ESCALATION REQUIRED

Do not weaken safeguards to force acceptance.

## 10. Proceed Instruction

After confirming the Repo Control Plane worktree is clean, proceed in the original hard-gate order:

    Gate A — reproduce and precisely classify
        ↓
    Gate B — minimal native structured-output probe
        ↓
    Gate C — actual production-schema probe
        ↓
    smallest evidenced compatibility correction
        ↓
    automated regression validation
        ↓
    controlled non-zero live validation
        ↓
    zero-delta live validation

Do not implement speculative fixes before Gate A classification.

Once the failure is classified and the prompt contracts are satisfied, make ordinary implementation choices locally without another pre-coding reconnaissance cycle unless a genuine scope/safety conflict appears.


# Product Owner / Architect Final Execution Clarification — Milestone 005.1

This clarification locks the remaining implementation details.

It does not change scope.

## 1. Exact Failure-Code Vocabulary

Use these exact literal provider/validation failure codes:

    provider_http_failure
    invalid_ollama_envelope
    missing_message
    missing_message_content
    invalid_message_content_type
    invalid_structured_content_json
    structured_content_validation_failed
    evidence_grounding_validation_failed

Do not substitute equivalent aliases.

These exact values should be shared by:

- provider implementation;
- CLI error presentation;
- automated tests;
- closeout reporting.

Future milestones may version or expand the taxonomy if needed.

## 2. Standard CLI Error Shape

For provider/structured-response failures, standardize operator-visible CLI output as:

    provider error [<code>]: <concise safe message>

Example:

    provider error [invalid_structured_content_json]: message.content was not valid JSON

The message must be concise and must not include:

- full Ollama response envelopes;
- `message.thinking` contents;
- hidden reasoning;
- arbitrary generated model content.

Use field-level extraction only.

## 3. Gate C Compatibility Changes Are Pre-Authorized Within the Locked Boundary

If:

    Gate B = PASS
    Gate C = FAIL

and evidence establishes an Ollama JSON-schema representation or provider/parser compatibility issue, the coder may implement the smallest compatible adjustment without another Product Owner checkpoint.

This permission includes:

- provider request-shape corrections;
- JSON-schema serialization corrections;
- semantically equivalent provider-facing schema representation changes;
- Ollama envelope parsing corrections;
- structured `message.content` parsing corrections.

The existing Repo Control Plane semantic validator must remain unchanged in meaning.

No additional approval is required when the resulting accepted model response still has exactly the Milestone 005 semantic contract.

STOP AND ESCALATE if success would require weakening:

- required fields;
- field types;
- enum restrictions;
- array/text bounds;
- evidence-ID grounding;
- zero-delta rules;
- non-zero-delta rules;
- local-only provider requirements;
- strict JSON parsing.

Compatibility adaptation is authorized.

Semantic-contract weakening is not.

## 4. Clean Preflight Must Be Enforced Now

Before any Gate A investigation or 005.1 code edit, perform the literal clean-worktree preflight.

Required evidence:

    git branch --show-current
    git rev-parse HEAD
    git status --short

If `git status --short` is non-empty:

    STOP

Report:

- branch;
- HEAD;
- exact status entries.

Do not:

- begin Gate A;
- edit files;
- reset;
- restore;
- clean;
- stage;
- commit;
- discard changes.

Proceed only when the initial Milestone 005.1 worktree is clean.

## 5. Thinking-Content Safety

Treat accidental serialization of `message.thinking` as a safety failure.

Provider implementation and tests should use explicit field extraction rather than logging or formatting complete Ollama message/envelope objects.

Automated tests must include a distinctive fake thinking string and prove that it does not appear in:

- returned provider errors;
- CLI-safe error messages;
- `analysis_input.json`;
- `analysis.json`;
- `analysis.md`;
- any other Milestone 005.1 persisted output.

Boolean evidence that thinking was present is permitted during bounded live validation.

The thinking content itself is never permitted.

## 6. Proceed

No further pre-coding clarification is required if the clean preflight passes.

Proceed in the locked order:

    clean Git preflight
        ↓
    Gate A failure classification
        ↓
    Gate B minimal native schema probe
        ↓
    Gate C production-schema probe
        ↓
    smallest evidenced compatibility correction
        ↓
    automated regression suite
        ↓
    non-zero live acceptance gate
        ↓
    zero-delta live acceptance gate

Ask for further Product Owner input only if a genuine scope, semantic-contract, or safety conflict is encountered.