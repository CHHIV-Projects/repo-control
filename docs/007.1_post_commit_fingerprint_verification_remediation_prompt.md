# Milestone 007.1 — Post-Commit Fingerprint Verification Remediation

Prompt file:

`docs/007.1_post_commit_fingerprint_verification_remediation_prompt.md`

Required closeout file:

`docs/007.1_post_commit_fingerprint_verification_remediation_closeout.md`

## Objective

Diagnose and remediate the M007 post-commit verification defect exposed by the real Linux Vocab App integration fixture during Milestone 009.1.

The defect occurred after:

- M008 staging succeeded;

- M007 prepare-commit succeeded;

- Git created exactly one local commit;

- the resulting repository was clean;

- M007 post-commit verification then reported:
  
    workflow error [post_commit_verification_failed]:
    committed delta does not match approved staged fingerprint

The resulting Vocab fixture commit is:

    64def13fd32fad6d320e6cfd0391955376918bbb

This milestone must determine exactly why the approved staged fingerprint and the resulting committed delta were considered different.

Do not assume the cause.

Do not loosen verification merely to make the test pass.

The goal is:

    one canonical representation
        ↓
    approved staged delta
        =
    resulting committed delta

for all Git states M007 claims to support.

Milestone 007.1 must preserve the M007 safety contract:

    explicit plan
    exact approval
    stale-plan rejection
    exact local commit
    deterministic post-commit verification
    no rollback/retry on uncertain success
    immutable external evidence

Milestone 009.1 remains PAUSED until this remediation passes.

---

# Repositories

## Repo Control Plane development repository

`/home/chuck/projects/repo-control`

## Preserved real-world evidence fixture

`/home/chuck/ai-agent-tests/vocab-app`

Vocab fixture branch:

`agent-test/repoctl-ui-demo`

Preserved resulting commit:

`64def13fd32fad6d320e6cfd0391955376918bbb`

The Vocab repository is READ-ONLY for this remediation unless a later gate explicitly authorizes otherwise.

Do not reset it.

Do not amend it.

Do not revert it.

Do not recreate the failed Gate J commit.

Do not make another Vocab commit during 007.1.

Do not push it.

Photo Organizer remains explicitly out of scope.

Do not access Photo Organizer.

---

# Preserved M009.1 Evidence

Use the following as controlling preserved evidence.

Context:

    synonym-handling--f504ea973173

Snapshots:

    baseline:
        snap--b6616211eed99ee8
    
    Change Set 1 unstaged:
        snap--1a6c1724ed6f7dfa
    
    staged:
        snap--3758057267506108

Comparison:

    cmp--f2c6f25db21d630b

Analysis:

    ana--d4fccab741db26cf

M008:

    stage plan:
        stage-plan--e6b8279bdf30b001
    
    stage execution:
        stage-exec--bd24d6839db5654d

M007:

    commit plan:
        commit-plan--6e195cd23738b1bd
    
    commit execution:
        none created
    
    resulting commit:
        64def13fd32fad6d320e6cfd0391955376918bbb

Do not delete, rewrite, manually edit, or fabricate any of these artifacts.

---

# Known Repo Control Plane Working State

Milestone 009.1 intentionally remains paused with one authorized pre-existing development change:

    M .gitignore

That change adds:

    *.egg-info/

It was introduced to prevent generated Python packaging metadata from contaminating the repository after installation into the permanent virtual environment.

Milestone 007.1 may begin with this one known modification present.

It is NOT part of 007.1.

Do not revert it.

Do not modify it further.

Do not include it in a 007.1 implementation commit.

If any other unexplained Repo Control Plane change is present before coding:

    STOP — ESCALATION REQUIRED

---

# Gate A — Development Preflight

In Repo Control Plane record:

    git branch --show-current
    git rev-parse HEAD
    git status --short

Verify the only pre-existing status entry before 007.1 implementation is:

    M .gitignore

Inspect the `.gitignore` diff only enough to confirm the known authorized addition is exactly the M009.1 hygiene change for:

    *.egg-info/

Record that result.

Verify no active Git operation is present.

At minimum inspect through established M006 state handling and verify absence of:

    MERGE_HEAD
    rebase-merge
    rebase-apply
    REBASE_HEAD
    CHERRY_PICK_HEAD
    REVERT_HEAD
    BISECT_LOG
    BISECT_START

If anything else is present:

    STOP AND REPORT

Do not clean unexplained state.

---

# Gate B — Preserved Vocab Evidence Preflight

Without mutating Vocab App, verify:

    branch:
        agent-test/repoctl-ui-demo
    
    HEAD:
        64def13fd32fad6d320e6cfd0391955376918bbb
    
    Git state:
        clean
    
    no active Git operation

Record the actual parent of:

    64def13fd32fad6d320e6cfd0391955376918bbb

Do not assume its parent from earlier notes.

Verify the commit exists and is attached to the expected branch.

Do not checkout another branch.

Do not create tags.

Do not alter refs.

---

# Gate C — Forensic Reconciliation Before Coding

Before modifying M007 code, determine exactly where the approved staged fingerprint and resulting committed delta diverge.

Use the preserved M007 commit plan and immutable evidence plus read-only Git inspection of the Vocab commit.

Trace the actual implementation path that produced:

    post_commit_verification_failed

Identify:

1. how the staged fingerprint was constructed at prepare time;
2. which exact fields are persisted in the commit plan;
3. how the post-commit committed delta is constructed;
4. how equality is evaluated;
5. the first exact field/value that differs.

Compare the approved and resulting representations at the semantic field level.

Examples of fields that may be relevant include:

    path
    status
    old object ID
    new object ID
    old mode
    new mode

but use the actual implementation contract rather than this list as an assumption.

Also determine whether any mismatch originates from:

    command construction
    parser behavior
    Git raw-diff format differences
    index-vs-tree semantics
    status normalization
    file-mode normalization
    object-ID placement
    added/deleted-file representation
    path encoding
    line-ending-related object behavior
    ordering
    parent/commit orientation
    another concrete cause

Do NOT assume CRLF/line endings are the cause merely because the Vocab repository has historically exercised Windows/Linux content.

Produce a concise diagnostic finding before implementing a fix.

If the actual mismatch cannot be identified deterministically:

    STOP — ESCALATION REQUIRED

Do not weaken comparison logic speculatively.

---

# Gate D — Safety Contract Classification

After identifying the first mismatch, classify it.

Determine whether the defect is:

A. equivalent Git state represented differently before vs after commit;

B. a parser/canonicalization bug;

C. an actual unsupported Git state that M007 incorrectly allowed;

D. evidence that the resulting commit genuinely differs from the approved staged state;

E. another concrete category.

If category D is found — the resulting Vocab commit genuinely contains a different semantic delta than the approved staged fingerprint:

    STOP — ESCALATION REQUIRED

Do not modify the verifier to accept it.

That would represent a substantive M007 safety failure requiring separate Product Owner review.

Proceed with remediation only if evidence establishes that the approved staged state and resulting commit are semantically equivalent and M007 is rejecting them because of a representation/verification defect, or another comparably bounded implementation defect.

---

# Gate E — Regression Reproduction

Before fixing production code, create a focused automated regression test that reproduces the defect.

Do NOT mutate the preserved Vocab repository to reproduce it.

Use an isolated temporary Git repository created by the test suite.

The regression fixture should reproduce the smallest concrete Git characteristics necessary to trigger the mismatch discovered in Gate C.

Do not copy unrelated Vocab application code into the test.

The test should demonstrate:

    prepare commit
        succeeds
    
    commit creation
        succeeds
    
    resulting repository
        contains the approved semantic delta
    
    pre-fix post-commit verification
        rejects that equivalent delta

The test must fail for the intended reason before the production fix.

Record the exact focused test.

---

# Gate F — Smallest Safe Remediation

Implement the smallest change that makes the pre-commit and post-commit representations obey one canonical semantic contract.

Preferred architectural direction:

    canonical staged-delta representation
        ↓
    commit plan fingerprint

and

    canonical committed-delta representation
        ↓
    same representation

followed by exact equality.

Do not introduce permissive comparison such as:

    ignoring object IDs
    ignoring modes
    ignoring status
    ignoring paths
    subset comparison
    unordered fuzzy matching

unless Gate C proves a specific field is non-semantic and the change is explicitly justified in the closeout.

Do not special-case:

    Vocab App
    app.py
    vocab_utils.py
    test_vocab_utils.py
    commit 64def13...
    CRLF filenames
    a specific branch

The fix must be general to the supported Git semantics.

Reuse one normalization/canonicalization implementation where practical rather than maintaining separate pre/post interpretations.

---

# Gate G — Preserve Unsupported-State Fail-Closed Behavior

Re-run and, where necessary, strengthen focused M007 tests proving unsupported Git states remain blocked.

At minimum preserve existing behavior for:

    conflicts
    active Git operations
    detached HEAD
    unstaged changes
    untracked changes at prepare time where prohibited
    stale plan
    changed branch
    changed HEAD
    changed staged fingerprint
    executable hooks/custom hooksPath boundary
    missing Git identity
    index lock
    unsupported/special file types where already governed
    post-commit verification mismatch

A true post-commit mismatch must still fail closed.

The remediation must distinguish:

    equivalent representation that should pass

from:

    genuinely different commit delta that must fail

---

# Gate H — Commit-Succeeded / Audit-Failed Semantics

Review the existing M007 handling for a Git commit that succeeds but post-commit verification fails.

Preserve the central rule:

    NEVER automatically retry the commit
    NEVER automatically reset/revert/amend the resulting commit

If post-commit verification fails after Git has produced a commit:

- report that mutation may/already did succeed;
- surface the resulting commit ID where known;
- preserve repository state;
- require operator investigation.

Do not convert this into a generic "commit failed" condition.

If the current error taxonomy already distinguishes this state appropriately, preserve it.

If the concrete 007.1 defect reveals that error reporting currently understates successful mutation, make only the smallest necessary reporting improvement.

Do not redesign the entire workflow error model.

---

# Gate I — Historical Vocab Artifact Recovery Decision

The preserved Vocab Gate J attempt created the commit but produced no commit execution artifact.

Do NOT fabricate a normal successful execution artifact retroactively.

Inspect whether the existing M007 architecture already provides a safe, deterministic, NON-GIT-MUTATING way to reconcile/audit a previously created commit against an immutable plan.

If such a mechanism already exists and can safely validate:

    repository identity
    branch
    plan integrity
    exact original parent
    exact resulting HEAD
    approved delta
    resulting committed delta
    clean post-state

then it may be used in read-only/audit mode and any evidence must clearly state that it is a reconciliation of a prior successful commit, not a newly executed commit.

If no such mechanism already exists:

    DO NOT add a new recovery command in 007.1 solely to manufacture missing M009.1 evidence.

The historical absence of the first execution artifact is acceptable defect evidence.

M009.1 can later obtain fresh successful M007 execution evidence from a subsequent controlled fixture commit after 007.1 passes.

This keeps 007.1 focused on the verifier defect.

---

# Gate J — Focused Tests

Run focused M007 tests including:

1. new regression reproducing the real defect;
2. ordinary successful commit;
3. stale plan;
4. genuine post-commit mismatch;
5. supported add/modify/delete behavior as relevant;
6. any exact Git characteristic implicated by Gate C.

Record:

    command
    test count
    pass/fail result

All must pass.

---

# Gate K — Full Repo Control Plane Regression

Run the complete Repo Control Plane test suite using the established environment.

Use the existing project test command, for example:

    PYTHONPATH=src python3 -m unittest -q

or the permanent virtual-environment equivalent if required.

Record:

    command
    test count
    result

Python-cache hygiene must remain effective.

Generated:

    __pycache__
    *.pyc
    *.egg-info

must not create tracked/untracked repository contamination under the intended ignore policy already present in the working tree.

Do not broaden hygiene scope.

---

# Gate L — Git-Operation Contamination Check

Immediately after full regression, verify the Repo Control Plane development repository has not acquired synthetic active-operation markers.

Check at minimum:

    MERGE_HEAD
    rebase-merge
    rebase-apply
    REBASE_HEAD
    CHERRY_PICK_HEAD
    REVERT_HEAD
    BISECT_LOG
    BISECT_START

If any appears:

    STOP — ESCALATION REQUIRED

Do not remove it automatically.

Record the result in the closeout.

---

# Gate M — Practical Disposable Commit Validation

Perform at least one practical end-to-end M007 validation using a NEW isolated disposable Git repository.

Do not use Vocab App for this mutation test.

Exercise the normal M007 lifecycle:

    create controlled repository
        ↓
    establish Git identity
        ↓
    create eligible staged change
        ↓
    snapshot as required by M007
        ↓
    prepare-commit
        ↓
    approve commit
        ↓
    exactly one Git commit
        ↓
    post-commit verification passes
        ↓
    execution evidence exists
        ↓
    repository clean

The practical fixture must exercise the exact condition that previously caused the Vocab failure, where reasonably reproducible.

Also perform a negative practical validation showing a true mismatch remains rejected if this can be done without dangerous or artificial production logic.

Record exact resulting IDs/evidence.

---

# Gate N — Read-Only Recheck of Preserved Vocab Commit

After the fix passes tests, use read-only diagnostic logic to re-evaluate the preserved Vocab evidence.

Do not rerun the commit.

Do not change Vocab HEAD.

Demonstrate that under the corrected canonical comparison:

    approved commit plan:
        commit-plan--6e195cd23738b1bd

and

    resulting commit:
        64def13fd32fad6d320e6cfd0391955376918bbb

represent the same approved semantic Git delta, IF that is in fact what Gate C established.

This validation may be performed through focused internal diagnostic/test code or bounded read-only invocation.

Do not create a fake historical normal-success artifact if the original execution never completed its audit.

Record the result.

If the corrected verifier still considers them genuinely different:

    STOP — ESCALATION REQUIRED

---

# Gate O — Final Development Repository State

At closeout, Repo Control Plane may contain:

- the pre-existing M009.1 `.gitignore` modification;
- intended 007.1 implementation changes;
- intended 007.1 tests;
- the 007.1 closeout.

No generated packaging/cache artifacts should remain visible.

Do not stage or commit implementation/closeout unless separately instructed.

Record literal:

    git status --short

Clearly distinguish:

    PRE-EXISTING M009.1 CHANGE:
        .gitignore

from:

    M007.1 CHANGES

Do not accidentally include `.gitignore` in a later 007.1 commit recommendation.

---

# Gate P — Vocab Fixture Preservation

At 007.1 closeout, verify Vocab remains exactly preserved:

    branch:
        agent-test/repoctl-ui-demo
    
    HEAD:
        64def13fd32fad6d320e6cfd0391955376918bbb
    
    status:
        clean

No new Vocab commits.

No reset.

No revert.

No amend.

No push.

Record literal:

    git status --short

---

# Implementation Scope

Expected implementation scope is narrow.

Likely areas include existing M007 files such as:

    src/repoctl/workflow/commit_plan.py
    src/repoctl/workflow/commit_execution.py
    related Git parsing/canonicalization helper(s)
    tests covering M007

Use actual repository structure.

Do not create parallel implementations.

Do not modify M008 unless Gate C proves a shared canonicalization defect genuinely belongs in a shared lower-level helper.

If touching M008 would change accepted staging behavior:

    STOP — ESCALATION REQUIRED

Do not modify browser code.

Do not modify GPT-OSS code.

Do not modify scanner/context/snapshot/comparison semantics except where an existing snapshot API is simply used by tests.

---

# Explicit Non-Goals

Do not implement:

- browser commit controls;
- browser stage controls;
- Git push;
- remote Git operations;
- automatic commit retry;
- automatic reset;
- automatic revert;
- automatic amend;
- historical execution-artifact fabrication;
- broad Git abstraction rewrite;
- selective staging;
- partial-hunk staging;
- Vocab product improvements;
- M009.1 Change Set 2;
- M009.1 layout work;
- Photo Organizer work.

---

# Escalation Protocol

STOP and report if:

- Repo Control Plane contains unexplained state beyond the known `.gitignore` change;
- Vocab fixture no longer matches preserved branch/HEAD/clean state;
- the approved staged delta and resulting Vocab commit are genuinely different;
- the first mismatch cannot be deterministically identified;
- remediation would require weakening safety checks;
- fix would require Vocab-specific behavior;
- fix materially changes M008 semantics;
- post-commit mismatch tests can no longer fail closed;
- full regression fails outside intended changed expectations;
- synthetic Git-operation markers reappear;
- Vocab mutation would be required;
- Photo Organizer access would be required.

Report:

1. exact gate;
2. observed state;
3. expected state;
4. exact first canonical mismatch;
5. whether the resulting historical commit is semantically equivalent to the approved staged delta;
6. Repo Control Plane mutations performed;
7. Vocab mutations performed;
8. test/evidence already produced;
9. rollback performed or not;
10. smallest next decision required.

Do not destroy preserved evidence.

---

# Required Closeout

Create:

`docs/007.1_post_commit_fingerprint_verification_remediation_closeout.md`

Include:

1. status — PASS or ESCALATION;
2. initial Repo Control Plane branch/HEAD/status;
3. confirmation that `.gitignore` was pre-existing M009.1 state;
4. initial Vocab branch/HEAD/status;
5. actual parent of historical Vocab commit;
6. commit plan inspected;
7. pre-commit staged-fingerprint construction path;
8. post-commit delta construction path;
9. exact first mismatching field/value;
10. root cause;
11. classification from Gate D;
12. why the historical commit is or is not semantically equivalent to the approved staged delta;
13. regression test added;
14. proof regression failed before production fix;
15. implementation files changed;
16. exact remediation;
17. canonicalization/equality contract after fix;
18. confirmation no permissive/fuzzy comparison introduced;
19. unsupported-state behavior preserved;
20. commit-succeeded/audit-failed behavior preserved;
21. historical artifact recovery decision;
22. focused test command/count/result;
23. full test command/count/result;
24. Python-cache/egg-info hygiene result;
25. post-regression Git-operation-marker result;
26. practical disposable M007 validation details;
27. disposable commit plan ID;
28. disposable execution ID;
29. disposable resulting commit SHA;
30. preserved Vocab read-only re-evaluation result;
31. confirmation historical Vocab commit was not retried;
32. confirmation no historical successful execution artifact was fabricated;
33. confirmation no Vocab mutation/push occurred;
34. confirmation no Photo Organizer access occurred;
35. literal final Vocab `git status --short`;
36. literal final Repo Control Plane `git status --short`;
37. exact 007.1 files recommended for eventual commit, explicitly excluding `.gitignore`;
38. recommendation for resuming M009.1.

---

# Acceptance Criteria

Milestone 007.1 passes only if:

- the real Vocab failure is explained deterministically;
- the exact first representation mismatch is identified;
- evidence establishes whether the historical commit actually matches the approved staged semantic delta;
- a focused regression reproduces the defect before the fix;
- the smallest general remediation is implemented;
- no Vocab-specific special case is introduced;
- approved staged state and resulting committed state use a consistent canonical comparison;
- genuine mismatches still fail closed;
- M007 never retries/resets/reverts/amends automatically after uncertain post-commit success;
- focused tests pass;
- full Repo Control Plane regression passes;
- no synthetic Git-operation markers remain;
- a fresh disposable end-to-end M007 commit succeeds and produces execution evidence;
- the preserved Vocab evidence is re-evaluated read-only under the corrected logic;
- the historical Vocab commit remains untouched;
- no fake historical execution evidence is created;
- the pre-existing M009.1 `.gitignore` change remains preserved and separate;
- no Vocab push occurs;
- no Photo Organizer access occurs.

Successful completion establishes that M007's post-commit verifier once again provides an exact, deterministic safety guarantee suitable for later browser exposure.

After 007.1 passes, Milestone 009.1 should resume from the preserved Vocab fixture state.

Do NOT return to Gate J by retrying the already-created commit.

Instead:

    preserved Gate J commit
        64def13fd32fad6d320e6cfd0391955376918bbb
            ↓
    007.1 proves/fixes verification defect
            ↓
    resume M009.1 at Gate K
            ↓
    continue Change Set 2 / forward correction
            ↓
    obtain fresh successful M007 execution evidence
        through a later authorized fixture commit

The 009.1 closeout must retain the original Gate J audit failure as historical evidence rather than pretending the first execution completed normally.
