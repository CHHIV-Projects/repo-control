# Milestone 005.1 Closeout — Ollama GPT-OSS Structured Output Compatibility

## 1. Status — PASS or ESCALATION

MILESTONE 005 ACCEPTANCE: PASS

MILESTONE 005.1: COMPLETE

## 2. Exact Original Failure Classification

Gate A reproduction using the legacy request shape (`think=false`) classified the first failing layer as:

- `invalid_structured_content_json`

Observed stage evidence:

- HTTP success: true
- outer Ollama envelope JSON parse: true
- `message.content` present and string: true
- `message.content` JSON parse: false

## 3. Root Cause Established by Evidence

The failure was request-contract compatibility, not transport or envelope parsing.

Evidence:

- With legacy `think=false`, the response reached `message.content` but failed structured JSON parsing (`invalid_structured_content_json`).
- With native compatible `think="low"`, Gate B minimal schema and Gate C production schema both passed.

Therefore the bounded remediation root cause is the GPT-OSS/Ollama thinking-mode contract mismatch.

## 4. Files Changed

- `src/repoctl/analysis/provider.py`
- `src/repoctl/analysis/manager.py`
- `src/repoctl/cli.py`
- `tests/test_analysis.py`

## 5. Exact Provider Request Before Remediation

Before remediation, provider request shape was:

- `model = "gpt-oss:20b"`
- `stream = false`
- `format = <JSON schema object>`
- `options.temperature = 0`
- `think = false`
- `tools` absent

## 6. Exact Provider Request After Remediation

After remediation, provider request shape is:

- `model = "gpt-oss:20b"`
- `stream = false`
- `format = <JSON schema object>`
- `options.temperature = 0`
- `think = "low"`
- `tools` absent

## 7. Minimal Structured-Output Probe Result

Gate B result:

- HTTP success: true
- outer envelope JSON: pass
- `message.content` present/type: pass
- structured content JSON parse: pass
- minimal schema validation: pass
- `message.thinking` present: true (boolean observation only)

## 8. Ollama Model Name/Digest

- model name: `gpt-oss:20b`
- digest: `17052f91a42e97930aa6e28a6c6c06a983e6a58dbb00434885a0cf5313e376f7`

## 9. Handling of `message.thinking`

`message.thinking` is ignored and discarded.

It is never persisted or rendered in:

- `analysis_input.json`
- `analysis.json`
- `analysis.md`
- CLI/provider diagnostics

Only boolean presence observation was used for live probe evidence.

## 10. Provider Parser/Error-Classification Changes

Implemented exact literal failure codes:

- `provider_http_failure`
- `invalid_ollama_envelope`
- `missing_message`
- `missing_message_content`
- `invalid_message_content_type`
- `invalid_structured_content_json`
- `structured_content_validation_failed`
- `evidence_grounding_validation_failed`

Provider behavior now parses in layers:

1. HTTP/network
2. outer envelope JSON
3. `message` object
4. `message.content` presence/type
5. `message.content` JSON syntax
6. semantic schema
7. grounding rules

CLI provider/structured-response failures now use:

- `provider error [<code>]: <concise safe message>`

## 11. Automated Test Results

Focused 005/005.1 tests:

- `PYTHONPATH=src python3 -m unittest tests.test_analysis`
- `Ran 24 tests ... OK`

Full suite:

- `PYTHONPATH=src python3 -m unittest`
- `Ran 77 tests ... OK`

Syntax check:

- `PYTHONPATH=src python3 -m py_compile ...`
- pass

## 12. Controlled Non-Zero Live Result

Fixture comparison:

- comparison_id: `cmp--9398fe9c590135dc`

Invocation outcome:

- attempt 1: success
- one request used (no retry loop)

Published artifacts:

- analysis_id: `ana--da33ea96e2dc21e1`
- output dir: `/home/chuck/.local/share/repoctl/m0051-fixture--ad247ad68b71/analyses/cmp--9398fe9c590135dc/ana--da33ea96e2dc21e1`

Observed output shape:

- review signal count: 5
- question count: 2
- evidence-grounded IDs present on signals

## 13. Representative Valid Review Signals/Evidence IDs

From published non-zero analysis:

- first signal evidence IDs: `["R001", "R002", "R003"]`

(Representative only; full content remains in immutable analysis artifacts.)

## 14. Zero-Delta Live Result

Fixture zero comparison:

- comparison_id: `cmp--99e8ebba1ea6e12c`

Invocation outcome:

- attempt 1: success

Published artifacts:

- analysis_id: `ana--6b9d9e47b0ba13fa`
- output dir: `/home/chuck/.local/share/repoctl/m0051-fixture--ad247ad68b71/analyses/cmp--99e8ebba1ea6e12c/ana--6b9d9e47b0ba13fa`

Observed zero-delta contract:

- `review_signals` count: 0
- `questions_for_human_review` count: 0

## 15. Confirmation Existing Grounding Contracts Remained Unchanged

Confirmed unchanged:

- semantic required fields
- enum restrictions
- bounds and text-shape checks
- evidence-ID existence/duplicate protections
- strict zero-delta emptiness rule
- non-zero minimum signal rule
- fail-closed invalid output behavior

## 16. Confirmation No Hidden Reasoning Was Stored

Confirmed:

- no `thinking` content present in published analysis artifacts
- provider diagnostics use safe field-level messages only

## 17. Target Repository Before/After Git Status

Disposable fixture used (not Vocab App).

After live analyze invocations:

- `git status --porcelain=v2 -z --untracked-files=all | wc -c` -> `0`

No target mutation occurred.

## 18. Limitations

- Validation is bounded to tested local environment and installed model digest.
- Structured-output behavior can vary across model/provider versions; taxonomy and fail-closed checks remain in place to preserve safety.

## 19. Whether Milestone 005 Acceptance Is Now PASS

Yes.

- MILESTONE 005 ACCEPTANCE: PASS

## 20. Recommended Next Milestone

Proceed to Milestone 006 planning/implementation, keeping the same deterministic authority model and fail-closed AI validation boundaries.

## 21. Final Repo Control Plane `git status --short`

At closeout completion:

- `M src/repoctl/analysis/manager.py`
- `M src/repoctl/analysis/provider.py`
- `M src/repoctl/cli.py`
- `M tests/test_analysis.py`
- `?? docs/005.1_ollama_gpt_oss_structured_output_compatibility_closeout.md`

No commit or push performed in this step.
