# Milestone 009.2 — Product Owner Review Reconciliation and UX Roadmap Reconnaissance

Prompt file:

docs/009.2_product_owner_review_reconciliation_recon_prompt.md

Required closeout file:

docs/009.2_product_owner_review_reconciliation_recon_closeout.md

## Milestone Mode

Reconnaissance-only.

No implementation changes.

Reasoning level: High.

## Goal

Translate the completed Product Owner hands-on review of the populated Repo Control Plane browser into a concrete, bounded implementation roadmap.

The review materially expanded the long-term Repo Control Plane vision.

This milestone must determine:

1. which requested improvements are presentation-only;
2. which can reuse existing deterministic data/services;
3. which require new read-only repository facts;
4. which require persistent metadata or new evidence models;
5. which require new workflow concepts such as task/milestone lineage;
6. which belong in M010 browser Stage/Commit;
7. which must remain later work.

The outcome must prevent the Product Owner design discussion from silently turning into one large implementation milestone.

# Repositories

Repo Control Plane:

    /home/chuck/projects/repo-control

Authorized read-only integration fixture:

    /home/chuck/ai-agent-tests/vocab-app

Vocab fixture expected branch:

    agent-test/repoctl-ui-demo

The intentional final M009.1 pending Vocab change must remain preserved:

    M vocab_utils.py

Do not modify, stage, commit, reset, restore, or clean the Vocab fixture during this reconnaissance.

Photo Organizer remains out of scope.

Do not access Photo Organizer.

# Required Starting State

Repo Control Plane must begin:

    correct branch
    clean working tree
    M009.1 committed

Vocab fixture must remain in the intentional M009.1 demonstration state.

If Repo Control Plane is unexpectedly dirty, or Vocab differs from the known intentional state:

    STOP AND REPORT

Do not clean unexplained state.

# Controlling Product Owner Findings

Treat the following as the controlling design findings from the completed M009.1 Product Owner review.

## A. General UI

- use more desktop width intelligently;
- reduce unnecessary left/right empty space;
- preserve readable prose widths;
- human-readable labels should lead;
- immutable machine IDs should remain available as secondary audit detail;
- normal operation should eventually avoid requiring visible CLI/server/tunnel management.

## B. Repository Selection

Current browser startup binds one repository.

Desired direction:

    controlled repository chooser

Requirements:

- choose only registered/approved repositories;
- no arbitrary browser filesystem path entry;
- switching repository changes Dashboard, Context, Snapshots, Comparisons, AI Review, and Workflow consistently.

## C. Dashboard — Git State

Current:

    Branch
    HEAD
    Workflow
    Git operation

Desired:

- human-readable commit information;
- include Git tags where useful;
- short SHA primary, full SHA available in detail;
- help/info controls explaining Workflow values;
- help/info controls explaining Git-operation states;
- better wrapping/layout for long branch names and identifiers.

## D. Dashboard — Working Tree

Current counts remain useful:

    Staged
    Unstaged
    Untracked
    Conflicts

Desired:

- Detail/View Files control;
- show which files belong to each category;
- preserve exact Git classification;
- this should later feed naturally into Stage review.

## E. Dashboard — Upstream

Current browser reports only locally known upstream state and performs no remote refresh.

Desired direction:

- current remote information should be obtainable through explicit fetch/refresh;
- show:
    upstream ref
    local relation
    ahead
    behind
    refresh state / freshness

Do not assume automatic background fetch is appropriate.

Determine the safest explicit refresh model.

Push remains later work.

## F. Snapshot Evidence

Current immutable snapshot IDs are technically correct but not human-friendly.

Desired human presentation includes concepts such as:

    Baseline before milestone
    Reviewed implementation
    Staged state
    Current pending state

Machine snapshot ID remains secondary evidence.

Investigate support required for:

- human-readable labels/purpose;
- branch/HEAD/state origin;
- Current vs Stale/Matching state;
- automatic or ensured snapshots at meaningful workflow boundaries;
- code-commit-to-code-commit lineage.

Do not assume existing snapshot schema contains enough information.

Determine what is already stored versus what would require new metadata.

## G. Code Commit → Code Commit Lineage

Major Product Owner requirement:

The meaningful development unit is the code transition:

    CODE COMMIT A
        ↓
    milestone work
        ↓
    reviewed state
        ↓
    CODE COMMIT B

Documentation-only prompt/addendum/closeout commits must not obscure the actual code evolution.

Investigate how existing:

    Git HEAD
    snapshot
    comparison
    stage plan
    commit plan
    commit execution

can be linked into this model.

Determine what is already derivable and what would require an explicit higher-level record.

## H. Comparisons

Current structural comparison concept is accepted.

Required UX direction:

- human-readable Before/After choices;
- machine snapshot IDs secondary;
- clearer relationship to code commits/states;
- prevent or explicitly reject reversed Before/After selection when Repo Control can prove ordering;
- do not rely solely on Git ancestry because multiple snapshots can exist at the same HEAD.

Determine what ordering/lineage information currently exists.

## I. Context

Current deterministic Context selection remains conceptually accepted.

It must remain:

    deterministic
    bounded
    non-AI-authoritative
    reproducible

Product Owner findings:

- human-readable context name;
- machine context ID secondary;
- show repository;
- show branch;
- show exact HEAD;
- show working-tree origin/state;
- show CURRENT versus STALE;
- stale context must not silently become coder authority;
- deduplicate repeated relationships and tests in the UI;
- retain Why Included evidence;
- distinguish current implementation from obvious historical/archive/backup copies where safely possible;
- default coder context should favor current authoritative implementation.

Investigate duplicate test/reference evidence seen in the Vocab `synonym` context.

Do not change deterministic matching semantics during reconnaissance.

## J. Coder Context Freshness

Major safety requirement:

Repository information supplied to a Coder must align with the exact code state the Coder is about to modify.

Desired conceptual contract:

    task context generated against:
        repository
        branch
        HEAD
        working-tree state

Before coding:

    expected state == current state

If not:

    CONTEXT STALE
    regenerate before coding

Determine which existing Repo Control identities/fingerprints can support this and what new binding is required.

## K. Architect Handoff

ChatGPT Architect does not directly access Repo Control Plane or server files today.

Desired future bridge:

    Architect Packet / Architect Handoff

Potential standard contents:

    repository
    branch
    HEAD
    working state
    milestone/task
    snapshot
    context query
    context ID
    relevant files
    relevant symbols
    relevant relationships
    related tests
    comparison/workflow evidence when relevant
    optional advisory GPT-OSS signals

For architecture requiring code inspection, an expanded packet may include bounded excerpts of selected relevant source.

Investigate how much of this information already exists and how an export could be produced without duplicating core evidence logic.

No direct ChatGPT/server integration is required now.

## L. AI Review

Current local GPT-OSS role is accepted:

    advisory only

Deterministic evidence remains authoritative.

Desired improvements:

- human-readable comparison/analysis presentation;
- machine IDs secondary;
- AI Summary;
- Review Signals;
- Questions for Human Review;
- evidence grounding retained;
- AI findings may later appear as secondary content in Architect/Coder handoffs.

AI must not become authoritative for context selection or Git promotion.

## M. Workflow

Current stage/commit audit history is technically useful but machine-oriented.

Desired presentation should emphasize:

    action
    starting code state
    files/count
    result
    execution status

rather than primarily:

    stage-plan--...
    commit-plan--...
    stage-exec--...
    commit-exec--...

Machine IDs remain available as audit details.

M010 remains expected to expose existing M008/M007 Stage/Commit behavior through reviewed browser approval controls.

## N. Whitespace / Formatting Control

Product Owner regularly encounters whitespace noise in prompts, addenda, and documentation.

Desired future capabilities:

Detection:

    trailing whitespace
    whitespace-only changes
    line-ending inconsistencies
    excessive/noisy blank-line changes where safely identifiable

Review:

    show all changes
    optionally hide whitespace-only differences visually

Cleanup:

    explicit user-approved normalization only

Never:

- silently ignore whitespace in the actual Git delta;
- silently rewrite files;
- stage a different result than the reviewed file state.

Investigate how existing `git diff --check` and Git diff mechanisms can support this safely.

## O. Stage / Commit / Push Direction

Long-term desired lifecycle:

    coder changes files
        ↓
    Repo Control review
        ↓
    whitespace / structural / test / optional AI evidence
        ↓
    Prepare Stage
        ↓
    Approve Stage
        ↓
    exact staged state
        ↓
    ensure reviewed snapshot
        ↓
    Prepare Commit
        ↓
    Approve Commit
        ↓
    verified local commit
        ↓
    refresh upstream
        ↓
    Prepare Push
        ↓
    Approve Push

M010 should remain bounded to browser Stage + Commit using existing M008/M007 authorities.

Push must remain a separate later guarded workflow.

## P. Higher-Level Milestone Record

Investigate a future first-class Milestone/Task Record that ties together:

    prompt
    code baseline
    fresh task context
    reviewed snapshot
    comparison
    tests/validation
    optional AI review
    stage plan/execution
    commit plan/execution
    resulting code commit
    closeout
    eventual push

Do not implement this record in 009.2.

Determine:

- which information already exists;
- which links are already deterministic;
- which links are currently only documented manually;
- what minimum new persistent metadata would be required.

## Q. Program Startup

Current development use requires:

    Repo Control web process
    SSH tunnel
    browser

Desired long-term operator experience:

    persistent Repo Control local service
        ↓
    browser shortcut/bookmark

CLI remains available for coder/debug/advanced use.

Investigate deployment/service options only enough to classify future work.

Do not implement service installation in 009.2.

# Reconnaissance Questions

For every Product Owner finding above, classify it as one of:

    1. presentation-only
    2. existing core data, new browser presentation
    3. bounded read-only core extension
    4. new persistent metadata/evidence model
    5. guarded Git mutation capability
    6. operational/deployment capability
    7. future architectural feature

For each finding identify:

- current implementation support;
- exact likely files/services involved;
- whether existing data is sufficient;
- missing data/identity if any;
- safety implications;
- recommended milestone placement.

# Required Roadmap Output

The closeout must recommend a bounded sequence.

At minimum evaluate this proposed structure:

    009.2
        reconnaissance only — this milestone
    
    009.3
        human-readable/read-only browser usability refinements
    
    010
        browser guarded Stage + Commit using M008/M007
    
    later
        automated snapshot/code lineage
    
    later
        task/context freshness + milestone record
    
    later
        Architect Packet
    
    later
        repository registry/chooser
    
    later
        upstream refresh + guarded Push
    
    later
        service-style startup

The coder may recommend a different order if current architecture shows a safer or simpler sequence.

Do not expand any proposed milestone into implementation during reconnaissance.

# Special Focus — 009.3 Boundary

Identify the largest coherent UX improvement that can be safely implemented before M010 without introducing a new major data model.

Prefer candidates such as:

- wider/responsive browser shell;
- human-readable short SHA/secondary full IDs;
- help/popovers;
- Working Tree file details;
- deduplicated Context display;
- improved human-readable Workflow history;
- human-readable snapshot/comparison labels derivable from existing facts;
- whitespace diagnostics display based on existing Git evidence.

Explicitly identify any desired feature that must NOT be placed in 009.3 because it requires:

- persistent labels;
- task/milestone model;
- snapshot lineage;
- repository registry;
- remote fetch;
- Git mutation.

# Validation

This is reconnaissance-only.

No implementation changes.

Allowed:

- targeted source inspection;
- read-only Repo Control CLI;
- read-only Git commands;
- read-only inspection of external Repo Control evidence;
- browser inspection;
- targeted Vocab fixture inspection without mutation.

Do not:

- stage;
- commit;
- push;
- fetch unless explicitly required only to inspect existing behavior and separately approved;
- modify Repo Control implementation;
- modify Vocab;
- create new Vocab evidence merely for this milestone;
- access Photo Organizer.

# Escalation

STOP and report if:

- Repo Control is unexpectedly dirty;
- Vocab intentional fixture state has changed unexpectedly;
- answering the roadmap requires destructive or mutating investigation;
- current implementation contradicts accepted M007/M008/M009 safety boundaries;
- reconnaissance reveals that M010 requires redesign of M007/M008 rather than browser adaptation;
- Photo Organizer access would be required.

# Required Closeout

Create:

docs/009.2_product_owner_review_reconciliation_recon_closeout.md

Include:

1. status;
2. exact Repo Control preflight;
3. exact Vocab fixture preservation state;
4. Product Owner findings matrix;
5. current implementation support for each finding;
6. presentation-only candidates;
7. read-only core-extension candidates;
8. persistent metadata/model candidates;
9. guarded Git capability candidates;
10. operational/deployment candidates;
11. Context duplicate-evidence finding;
12. Context freshness architecture finding;
13. snapshot/code-lineage finding;
14. Milestone Record finding;
15. Architect Packet finding;
16. repository chooser finding;
17. upstream/fetch finding;
18. whitespace-control finding;
19. M010 Stage/Commit boundary confirmation;
20. recommended 009.3 scope;
21. recommended post-M010 roadmap;
22. exact likely files/services for 009.3;
23. explicit deferred items;
24. confirmation no implementation mutation occurred;
25. confirmation Vocab was not changed;
26. confirmation no Photo Organizer access occurred;
27. literal final Repo Control `git status --short`;
28. literal final Vocab `git status --short`.

# Definition of Done

009.2 is complete only when the Product Owner review findings have been converted into a concrete architecture and milestone roadmap without implementation scope creep.

The closeout must make it possible to write 009.3 as a focused implementation prompt and preserve M010 as the separately bounded browser Stage/Commit milestone.




Confirmed. Use the following decisions for Milestone 009.2.

1. STARTING-STATE GATE

Enforce Gate A strictly.

Do NOT begin 009.2 reconnaissance while Repo Control Plane still contains uncommitted M009.1 implementation/closeout work.

Required start:

    M009.1 committed
    Repo Control Plane working tree clean
    correct branch
    009.2 prompt committed
    no active Git operation

If that state has not yet been reached:

    STOP

Do not draft reconnaissance against the dirty intermediate state and merely document the gate as a precondition.

The purpose of 009.2 is to analyze the accepted M009.1 architecture from a stable Git boundary.

If the only issue is that the approved M009.1 closeout/CSS/.gitignore changes have not yet been committed, report that exact state and wait for the Product Owner to complete the already-approved M009.1 commit.

2. PRODUCT OWNER FINDINGS MATRIX

Use one primary row for each major section A through Q.

Within each row, summarize the individual bullet findings as needed.

Preferred columns:

    Finding
    Current Support
    Classification
    Existing Data Sufficient?
    Likely Files / Services
    Safety / Architecture Implication
    Recommended Milestone

Do not create one matrix row for every individual bullet unless materially different bullets require different architectural classifications.

The goal is a readable architectural map, not an unnecessarily large spreadsheet-like closeout.

Where one A–Q section contains findings that clearly belong to different categories, split that row into bounded subrows such as:

    F1 — human-readable snapshot presentation
    F2 — automatic snapshot creation
    F3 — code lineage

because those may legitimately have different implementation boundaries.

3. ROADMAP ORDERING

Yes.

Treat the roadmap proposed in the prompt as the default baseline:

    009.3
        read-only / human-readable browser usability

    010
        browser guarded Stage + Commit

    later
        snapshot/code lineage
        task/context freshness + milestone record
        Architect Packet
        repository registry/chooser
        upstream refresh + guarded Push
        service-style startup

Deviate only when reconnaissance identifies a concrete architecture, dependency, or safety reason.

If recommending a different order:

- identify the specific dependency;
- explain why the baseline order would be unsafe or inefficient;
- recommend the smallest revised sequence.

Do not reorder merely for preference.

4. CONTEXT DUPLICATE-EVIDENCE INVESTIGATION

Yes.

Constrain the investigation to the already-existing Vocab synonym context evidence.

Use the known context:

    synonym--ee6b5af5c217

and other already-preserved synonym context evidence where useful.

Do NOT generate a new context pack merely for 009.2.

Determine why the existing output contains repeated:

    test_reference relationships
    Related Tests entries

and classify whether the duplication originates in:

    underlying deterministic evidence
    context assembly
    relationship representation
    browser/view-model rendering
    template rendering
    another concrete layer

Do not fix it.

The 009.2 closeout should identify the appropriate remediation layer so 009.3 does not accidentally hide a core-data defect with presentation-only deduplication.

5. 009.3 BOUNDARY

Your initial assessment is aligned with the intended boundary.

Prefer 009.3 for improvements that can be derived safely from already-existing facts, such as:

    human-readable labels
    short-primary / full-secondary identifiers
    layout improvements
    explanatory help
    Working Tree file details
    Context presentation/deduplication if evidence proves it is a view concern
    more understandable Workflow presentation
    snapshot/comparison presentation derived from existing facts
    whitespace diagnostics derived from existing Git evidence

Defer anything that requires a significant new authority/model, including:

    repository registry/chooser
    persistent snapshot purpose labels if new persistence is required
    code-transition lineage
    task/context freshness binding
    Milestone Record
    Architect Packet
    remote fetch/upstream mutation workflow
    guarded Push
    service-style startup

If reconnaissance proves one of these is substantially simpler and already supported by existing data, document that finding; do not implement it.

6. M010 BOUNDARY

Keep M010 intact:

    Browser guarded Stage + Commit

Reuse the accepted M008 and M007 core services.

009.2 must specifically confirm whether the browser can expose those services without redesigning their safety contracts.

If M010 would require modifying fundamental M007/M008 authority or semantics:

    STOP / ESCALATE

rather than folding that redesign into M010.

7. CLOSEOUT FORMAT

Use the exact 28 required closeout subjects from the prompt.

The A–Q findings matrix can satisfy the central architecture mapping, followed by focused sections for the major findings required by the closeout.

Capture findings while reconnoitering rather than reconstructing them afterward.

Keep the closeout detailed enough to make 009.3 and M010 prompts straightforward, but do not repeat the same finding across many sections unless the later section adds a specific architectural conclusion.

STATUS:

    009.2 DESIGN DECISIONS CONFIRMED
    STRICT CLEAN START REQUIRED
    ROADMAP BASELINE ACCEPTED
    EXISTING CONTEXT EVIDENCE ONLY
    NO IMPLEMENTATION AUTHORIZED

    009.2 closeout is substantially approved, with one clarification required before final acceptance.

Please revisit Section 11 — Context duplicate-evidence finding.

The Product Owner's actual browser output for:

    synonym--ee6b5af5c217

visibly showed repeated identical-looking entries within the Related Tests section itself, including:

    test_ordering_independent
        displayed six times

and several other tests displayed twice.

The closeout currently states:

    "duplicate rows within each section: none"

and concludes that the perceived duplication is cross-section representation.

Please reconcile those two observations.

Specifically determine, read-only:

1. Whether those visually identical Related Tests entries are actually distinct deterministic records because of hidden fields, source relationships, evidence identities, or other metadata not rendered by the browser.

2. If they are distinct, identify exactly what distinguishes them and explain why the current UI makes them appear identical.

3. If they are not materially distinct, correct Section 11 to identify the actual duplication layer.

4. State the safest 009.3 presentation rule:
   - preserve distinct evidence but aggregate its browser presentation;
   - deduplicate genuinely redundant rows;
   - or another precisely supported treatment.

Do not change implementation.

Do not generate new context evidence.

Use the existing preserved synonym context only.

Update the closeout as needed so that 009.3 does not accidentally hide meaningful deterministic evidence while trying to improve readability.

All other 009.2 findings and roadmap recommendations are accepted pending this clarification.