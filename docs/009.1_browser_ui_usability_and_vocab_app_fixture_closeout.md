# Milestone 009.1 Browser UI Usability and Vocab App Fixture Closeout

## 1. status

PASS

## 2. scope result summary

Milestone 009.1 completed with the browser still read-only for Git mutation, while using the authorized Vocab fixture to populate meaningful Dashboard, Context, Snapshots, Comparisons, AI Review, and Workflow evidence.

The previously escalated Gate J historical failure was preserved, not rewritten, and remediation was completed in Milestone 007.1 before resuming 009.1 from Gate K.

The historical fixture story is therefore split across two phases:

- the original M009.1 evidence run, which produced the baseline, Change Set 1, the first staged transaction, and the Gate J historical commit audit failure;
- the resumed M009.1 run, which continued from the preserved fixture branch, generated the review-rich Change Set 2 evidence, performed the forward correction, left the final harmless pending change, and populated the browser pages for Product Owner review.

For the repo-control development repository, the Gate A story has three distinct evidence points:

- original M009.1 preflight: branch `main`, HEAD `b0af1486ea3acea873e8394d1bc039c3d8ebd0c8`, status `?? src/repoctl.egg-info/`
- authorized hygiene transition: remove only generated `src/repoctl.egg-info/` and add `*.egg-info/` to `.gitignore`
- resumed M009.1 state after 007.1 interruption/resumption: known authorized `.gitignore` hygiene state carried forward; the closeout does not restate a resumed HEAD because that exact resumed HEAD was not separately recorded in the captured evidence used here

## 3. implementation changes in Repo Control Plane

Modified:

- `.gitignore` (pre-existing authorized state retained)
- `src/repoctl/web/static/app.css`

Added:

- `docs/009.1_browser_ui_usability_and_vocab_app_fixture_closeout.md`

No scanner/context/snapshot/comparison/analysis/workflow semantics were changed.
No browser Git mutation controls were added.

The only presentation refinement in this milestone was the desktop-width CSS adjustment in `src/repoctl/web/static/app.css`.

## 4. gate-by-gate outcome

### Gate A (Repo Control Plane preflight)

Original M009.1 preflight evidence was:

- branch: `main`
- HEAD: `b0af1486ea3acea873e8394d1bc039c3d8ebd0c8`
- literal status: `?? src/repoctl.egg-info/`

The authorized hygiene transition then removed only the generated `src/repoctl.egg-info/` directory and added `*.egg-info/` to `.gitignore`.

The resumed M009.1 state after the 007.1 interruption/resumption was the known authorized `.gitignore` hygiene state carried forward from that transition.

No destructive cleanup was performed.

### Gate B (Vocab preflight)

Original Vocab App fixture preflight evidence was:

- original branch: `agent-test/gpt-oss-synonym-refactor`
- original HEAD: `522a1e83e4de876330c018e6f771b77de3b5b011`
- original status: clean

The dedicated M009.1 fixture branch was then used for the disposable demo work.

### Gate C (sensitive boundary)

Credential/sensitive boundary checks completed by filename/path only; no credential content inspection used.

### Gate D (fixture branch)

Branch in use:

- `agent-test/repoctl-ui-demo`

Baseline/reference commit recorded:

- `522a1e83e4de876330c018e6f771b77de3b5b011`

No force-delete/reset/rewrite used.

This branch was treated as the dedicated local demo branch for the entire fixture sequence.

### Gate E (baseline evidence)

Baseline evidence captured from the original execution:

- baseline snapshot ID: `snap--b6616211eed99ee8`
- context/query ID: `synonym-handling--f504ea973173`

That context result established that the Vocab fixture’s synonym area was already understood in deterministic terms, with relevant files, symbols, relationships, and tests selected around synonym-handling behavior rather than an unrelated feature area.

### Gate F/G (Change Set 1 + snapshot/comparison/AI)

Change Set 1 was a small synonym-area refactor intended to produce realistic Repo Control Plane evidence. The work touched the synonym implementation and its nearby tests rather than adding a disconnected dummy module.

Affected files:

- `app.py`
- `vocab_utils.py`
- `test_vocab_utils.py`

Validation result:

- 12 Vocab tests passed

Recorded evidence IDs:

- unstaged snapshot: `snap--1a6c1724ed6f7dfa`
- comparison: `cmp--f2c6f25db21d630b`
- analysis: `ana--d4fccab741db26cf`

The resulting evidence showed a meaningful structural comparison and a real local GPT-OSS analysis over the synonym-area refactor.

### Gate H/I (M008 staging + staged snapshot)

The first M008 transaction from the original execution was:

- stage plan: `stage-plan--e6b8279bdf30b001`
- stage execution: `stage-exec--bd24d6839db5654d`
- staged snapshot: `snap--3758057267506108`

This was the first guarded staging history entry used to populate Workflow evidence.

### Gate J (M007 commit, historical escalation)

Historical Gate J produced the commit plan:

- commit plan: `commit-plan--6e195cd23738b1bd`

Historical normal execution artifact:

- none

Historical resulting commit:

- `64def13fd32fad6d320e6cfd0391955376918bbb`

The post-commit audit failure was later diagnosed and fixed in Milestone 007.1.
That remediation established that the historical commit itself was semantically equivalent to the approved staged plan under the corrected canonicalization contract.
No retry, reset, revert, amend, or backfill occurred for the historical Vocab commit.

### Gate K (Change Set 2 review-rich state)

Change Set 2 was the deliberately more review-rich synonym-area state used to generate a materially different comparison from Change Set 1. The implementation moved toward a synonym ranking policy in `synonym_policy.py` and test coverage that made the output more debatable for GPT-OSS review without introducing credentials or external dependencies.

Recorded IDs:

- snapshot: `snap--9a7ef144c82775b7`
- comparison: `cmp--4e730c3669a8077c`
- analysis: `ana--cd8c68933885f52a`

The state was questionable because the synonym ranking policy changed observable behavior in a bounded way that intentionally invited review rather than representing a final polished product decision. No deliberate test failure was required for this gate; the evidence goal was structural richness, not an intentionally broken fixture.

### Gate L (forward correction to healthy state)

The forward correction kept the historical evidence but repaired the questionable synonym behavior so the fixture returned to a healthy demonstration state.

Recorded IDs:

- stage plan: `stage-plan--6ba9200c8955d818`
- stage execution: `stage-exec--5ecc83baf3ccaded`
- staged snapshot: `snap--0275a6997f42d724`
- commit plan: `commit-plan--44924c24fc86dd87`
- commit execution: `commit-exec--794110d709a5c85b`
- resulting commit SHA: `5a251f14a7a69ee2dfc9fc72312f49b0c001c30e`

This correction preserved the synonym-policy history while restoring the phrase-visibility behavior used for the final demonstration branch.

Vocab tests passed at the healthy-state checkpoint.

### Gate M (intentional final pending change)

Final fixture intentionally left with one harmless unstaged change.

The final pending edit in `vocab_utils.py` was a small normalization-related change that stayed within synonym handling and did not break the application. It is harmless because it is narrow, testable, and suitable for M010’s future browser approval flows without needing any hidden external state.

Literal final fixture status:

```text
 M vocab_utils.py
```

Workflow state:

- `unstaged_only`

### Gate N (populate every browser area)

Live runtime validation executed against:

- repository: `/home/chuck/ai-agent-tests/vocab-app`
- host: `127.0.0.1`
- validated runtime port: `8870` (used because `8765` was occupied during validation)

All six pages confirmed populated with meaningful data:

- `dashboard_populated=1`
- `context_populated=1`
- `snapshots_populated=1`
- `comparisons_populated=1`
- `ai_review_populated=1`
- `workflow_populated=1`
- `browser_mutation_controls_absent=1`

AI Review validation included:

- local GPT-OSS section visible
- advisory labeling visible
- evidence identifiers visible (for example `C002`, `F001`, `S001`, `T001`)
- hidden thinking not rendered

Visible page content at runtime was:

- Dashboard: real branch/HEAD values, non-clean workflow state, staged/unstaged counts, and snapshot candidate / matching snapshot information were rendered.
- Context: a meaningful synonym-handling query was visible with relevant files, symbols, relationships, and tests.
- Snapshots: multiple known snapshots were listed for the fixture.
- Comparisons: multiple meaningful comparison entries were visible, including non-zero structural counts/details.
- AI Review: the live local GPT-OSS analysis page showed advisory review text, grounded evidence IDs, and the selected comparison/analysis context.
- Workflow: stage plans and commit plans/executions from the fixture history were visible, including earlier and later transactions.

### Gate O (desktop width/layout improvement)

Completed via bounded CSS-only refinement in `src/repoctl/web/static/app.css`:

- shell width now uses approximately desktop-responsive `95vw` capped at `1320px`
- repository card width expanded for long path readability
- panel horizontal overflow handling for wide populated content
- table/list/path wrapping improvement for long IDs/paths
- prose width bounded for readability

### Gate P (presentation-only refinement)

Completed with population-driven layout fixes only.
No deterministic terminology or core semantics changed.

### Gate Q (Product Owner review environment)

Primary launch command:

```bash
/home/chuck/.venvs/repo-control/bin/repoctl web \
  --repository /home/chuck/ai-agent-tests/vocab-app \
  --host 127.0.0.1 \
  --port 8765
```

If local port 8765 is occupied, use an alternate local port (for example `8870`) consistently in both SSH tunnel and browser URL.

Windows tunnel command:

```bash
ssh -N -L 8765:127.0.0.1:8765 chuck@192.168.1.173
```

Browser URL:

```text
http://127.0.0.1:8765
```

## 5. tests and validations

### Repo Control Plane focused web tests

Command:

```bash
/home/chuck/.venvs/repo-control/bin/python -m unittest tests.test_web -q
```

Result:

- `Ran 12 tests`
- `OK`

### Repo Control Plane full regression

Command:

```bash
/home/chuck/.venvs/repo-control/bin/python -m unittest -q
```

Result:

- `Ran 142 tests`
- `OK`

### Vocab healthy-state tests

Command used during Gate L path:

```bash
python3 -m unittest -q
```

Result:

- `Ran 14 tests`
- `OK`

The original Change Set 1 evidence run also recorded a 12-test passing result for the synonym refactor before the later healthy-state correction path.

## 6. Git operation marker check (post-regression)

Development repository marker check result:

- `MERGE_HEAD=absent:.git/MERGE_HEAD`
- `rebase-merge=absent:.git/rebase-merge`
- `rebase-apply=absent:.git/rebase-apply`
- `REBASE_HEAD=absent:.git/REBASE_HEAD`
- `CHERRY_PICK_HEAD=absent:.git/CHERRY_PICK_HEAD`
- `REVERT_HEAD=absent:.git/REVERT_HEAD`
- `BISECT_LOG=absent:.git/BISECT_LOG`
- `BISECT_START=absent:.git/BISECT_START`

All required markers absent.

The marker check confirmed no synthetic Git operation state remained active in the development repository after the regression run.

## 7. no-remote-mutation confirmation

Fixture work remained local only.
No push/force-push was performed.
No remote branch creation was required.

## 8. final literal statuses

Final Repo Control Plane status:

```text
 M .gitignore
 M src/repoctl/web/static/app.css
?? docs/009.1_browser_ui_usability_and_vocab_app_fixture_closeout.md
```

Final Vocab fixture status:

```text
 M vocab_utils.py
```

The fixture intentionally remains dirty with this known harmless unstaged change so M010 has a natural pending browser approval candidate.

Final Vocab fixture branch and HEAD:

- branch: `agent-test/repoctl-ui-demo`
- HEAD: `5a251f14a7a69ee2dfc9fc72312f49b0c001c30e`

Recent fixture commits:

- `5a251f1` M009.1 demo commit 2: synonym policy forward correction
- `64def13` M009.1 demo commit 1: synonym helper refactor

## 9. hygiene, safety, and deferred items

Python cache / egg-info hygiene remained effective during the milestone; no cache or `*.egg-info` noise remained in the final meaningful validation state.

No Photo Organizer access occurred.

Browser Stage/Commit mutation remains deferred to M010.

The historical first M007 execution artifact remains absent by design; no fabricated recovery artifact was introduced.

M010 should be composed only after Product Owner hands-on review of the populated browser, and then may expose M008/M007 through reviewed, plan-specific browser approval flows.
