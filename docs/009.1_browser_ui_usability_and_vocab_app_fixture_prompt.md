# Milestone 009.1 — Browser UI Usability and Vocab App Integration Fixture

Prompt file:

`009.1_browser_ui_usability_and_vocab_app_fixture_prompt.md`

Required closeout file:

`009.1_browser_ui_usability_and_vocab_app_fixture_closeout.md`

## Objective

Turn the Milestone 009 browser foundation into a genuinely useful Product Owner evaluation environment.

Milestone 009 proved that the browser architecture works.

Milestone 009.1 must now prove that the browser remains understandable and useful when populated with realistic repository evidence.

This milestone has two bounded goals:

    A. improve desktop use of available screen width
    
    B. intentionally use the disposable Linux Vocab App as a
       mutable Repo Control Plane integration fixture

The Vocab App fixture will be used to generate meaningful:

    repository states
    context results
    snapshots
    structural comparisons
    GPT-OSS analyses
    stage plans/executions
    commit plans/executions

so the Product Owner can evaluate the actual browser experience before browser Git mutation controls are introduced in Milestone 010.

Milestone 009.1 does NOT add browser Stage or Commit buttons.

---

# Projects

## Repo Control Plane development repository

`/home/chuck/projects/repo-control`

This remains the application being developed.

Normal development safety rules apply.

It must start clean.

Do not use the Repo Control Plane development repository as the mutation/demo fixture.

## Authorized mutable integration fixture

Linux Vocab App copy:

`/home/chuck/ai-agent-tests/vocab-app`

For Milestone 009.1 ONLY, this Linux Vocab App copy is explicitly authorized as a disposable/mutable integration fixture.

It may be:

- edited;
- structurally changed;
- intentionally placed into different Git states;
- tested;
- temporarily broken;
- snapshotted;
- compared;
- analyzed;
- staged through Repo Control Plane M008;
- committed through Repo Control Plane M007.

Do NOT access or modify any other copy of Vocab App.

Do NOT push Vocab App fixture changes to a remote.

Photo Organizer remains explicitly out of scope.

Do not inspect, scan, modify, or otherwise access Photo Organizer.

---

# Controlling Architecture

Preserve Milestones 001–009.

The hierarchy remains:

    repository source / Git / tests
        ↓
    deterministic Repo Control Plane facts
        ↓
    immutable snapshots / comparisons / workflow evidence
        ↓
    advisory GPT-OSS
        ↓
    human Product Owner judgment

The browser remains an adapter over existing Repo Control Plane core services.

M009.1 must not introduce a second implementation of:

- Git state;
- scanning;
- context generation;
- snapshots;
- comparisons;
- GPT-OSS analysis;
- staging;
- commits.

---

# Hard Browser Mutation Boundary

M009.1 does NOT change the Milestone 009 browser mutation boundary.

The browser must remain unable to execute:

    stage
    commit
    push
    fetch
    pull
    reset
    restore
    clean
    stash
    branch mutation
    merge
    rebase
    cherry-pick
    revert

M008 and M007 may be exercised against the Vocab App fixture through their existing CLI/core paths.

Browser mutation controls remain deferred to M010.

---

# Gate A — Repo Control Plane Development Preflight

Before any implementation:

    cd /home/chuck/projects/repo-control
    git branch --show-current
    git rev-parse HEAD
    git status --short

Also establish that no Git operation is active using the existing M006 state machinery or equivalent verified evidence.

Require:

    clean development repository
    no active Git operation
    M009 committed

If not clean:

    STOP

Do not reset, restore, stash, clean, stage, or discard unexplained development-repository state.

Do not commit or push unless separately instructed.

---

# Gate B — Vocab App Fixture Preflight

Before mutating Vocab App:

    cd /home/chuck/ai-agent-tests/vocab-app
    git branch --show-current
    git rev-parse HEAD
    git status --short

Require:

    valid Git repository
    attached branch
    clean worktree/index
    no active Git operation

Record:

    original branch
    original HEAD

This exact commit is the immutable reset/reference point for the fixture.

Do not force-reset the original branch.

Do not rewrite its history.

If Vocab App is already dirty:

    STOP AND REPORT

unless the state is clearly documented pre-existing fixture state from a prior authorized Repo Control Plane test.

Do not guess.

---

# Gate C — Credential / Sensitive File Boundary

The Linux Vocab App is intended to be a sanitized test copy.

Before mutation, verify by filename/path only that obvious sensitive credential files are not tracked or present.

At minimum check for known sensitive forms such as:

    service_account.json
    .streamlit/secrets.toml
    credential/key files previously excluded from the Linux fixture

Do NOT open or print credential contents.

If an actual credential-bearing file is found:

    STOP — ESCALATION REQUIRED

Do not proceed with fixture mutation until the sensitive-data boundary is understood.

---

# Gate D — Dedicated Fixture Branch

Create and use a dedicated local branch for M009.1 fixture work.

Preferred branch:

    agent-test/repoctl-ui-demo

Create it from the exact clean Vocab App baseline recorded in Gate B.

Do not mutate the original Vocab App branch.

Do not push this fixture branch.

If `agent-test/repoctl-ui-demo` already exists:

- inspect it;
- do not force-delete it;
- do not reset it automatically;
- continue only if it is clearly the previously authorized Repo Control Plane demo branch and its state is understood.

Otherwise:

    STOP AND REPORT

Record the baseline commit explicitly in the closeout.

A separate tag is not required.

The recorded baseline SHA is sufficient as the reset reference.

---

# Gate E — Baseline Repo Control Plane Evidence

Before modifying Vocab App on the dedicated fixture branch, capture a meaningful clean baseline.

At minimum:

    repoctl milestone status
    repoctl snapshot

using the permanent Repo Control Plane environment.

Record:

    branch
    HEAD
    workflow state
    baseline snapshot ID

Where useful, also run a deterministic context query against a real feature area to verify the fixture remains understood by M003.

Prefer a feature area already meaningful in the application, such as vocabulary/synonym functionality, if current code supports it.

Do not broadly reconnoiter unrelated application areas.

---

# Gate F — Realistic Controlled Change Set 1

Create a small but structurally meaningful Vocab App change.

The purpose is NOT to improve Vocab App as a product.

The purpose is to produce realistic Repo Control Plane evidence.

Prefer a bounded change involving approximately:

    2–4 production/test files

with structural effects such as:

    add or extract helper
    change a call relationship
    add/update related test
    add a small internal module if justified

Use existing code structure rather than inventing a disconnected dummy module.

Do not introduce credentials, network integrations, or destructive behavior.

The change should be understandable enough that GPT-OSS has something meaningful to review.

Run appropriate Vocab App tests.

Record whether they pass.

---

# Gate G — Snapshot / Comparison / AI Evidence for Change Set 1

While Change Set 1 is present, generate:

    snapshot
    comparison from baseline
    GPT-OSS analysis

Record exact:

    snapshot ID
    comparison ID
    analysis ID

The comparison should contain real non-zero structural evidence.

The GPT-OSS analysis must use the existing M005/M005.1 local provider contract.

Do not change model settings.

Do not expose hidden thinking.

Do not use AI to decide whether the fixture change should be committed.

---

# Gate H — Exercise M008 Guarded Staging

Use the existing Repo Control Plane M008 workflow against the Vocab App fixture.

The intended lifecycle is:

    unstaged Change Set 1
        ↓
    repoctl milestone prepare-stage --all
        ↓
    inspect immutable stage plan
        ↓
    repoctl milestone stage <plan-id> --approve
        ↓
    staged_only

Record:

    stage plan ID
    stage execution ID
    resulting workflow state

This is explicitly authorized fixture mutation.

Do not stage manually unless required to recover from an implementation defect, in which case STOP and report first.

The purpose is to populate real Workflow evidence using the actual M008 service.

---

# Gate I — Snapshot Staged State

After successful M008 staging:

    repoctl snapshot

Record the exact snapshot ID.

This snapshot should satisfy the M007 matching-snapshot contract.

---

# Gate J — Exercise M007 Guarded Commit

Use M007 against the fixture:

    repoctl milestone prepare-commit
        --message "<bounded demo commit message>"
        ↓
    inspect plan
        ↓
    repoctl milestone commit <plan-id> --approve

Record:

    commit plan ID
    commit execution ID
    resulting commit SHA
    resulting workflow state

Verify:

    exactly one local commit
    expected parent
    expected message
    clean repository after commit
    no push

This creates real workflow history for the browser Workflow page.

---

# Gate K — Controlled Change Set 2 / Review-Rich State

Create a second structurally meaningful change after Change Set 1 is committed.

This change should produce a noticeably different comparison from Change Set 1.

It may include, for example:

    function movement or extraction
    call relationship change
    added or removed test reference
    added small helper/module
    intentionally questionable but bounded implementation choice

It is acceptable to temporarily create a failing test or deliberately imperfect code state for evidence generation.

However:

- do not damage system configuration;
- do not create an irreversible state;
- do not introduce credentials;
- do not affect any repository outside the Vocab App fixture.

Capture a snapshot of this review-rich state.

Compare it with the prior meaningful snapshot.

Run GPT-OSS analysis.

Record all IDs.

If the state intentionally causes a test failure, record that fact clearly.

Do not rely on Repo Control Plane currently displaying runtime test failures unless that capability already exists.

The primary goal remains structural/evidence richness.

---

# Gate L — Restore Fixture to a Healthy Demonstration State

Do not leave Vocab App in an accidentally broken or ambiguous state.

After generating any deliberately questionable/broken comparison evidence:

- make a bounded forward correction;
- do not erase historical evidence;
- do not rewrite prior fixture commits;
- run the applicable Vocab App tests.

Use M008 + snapshot + M007 again where practical so the Workflow page gains another realistic transaction history.

Record resulting:

    stage plan/execution
    snapshot
    commit plan/execution
    commit SHA

The fixture history should now contain multiple meaningful states.

---

# Gate M — Intentional Final Pending Change

After healthy fixture commits are established, create one final small, harmless, testable change and intentionally leave it UNSTAGED.

Purpose:

- Dashboard should show a non-clean current workflow state;
- Workflow should have historical stage/commit evidence;
- M010 will later have a natural change available for browser Stage approval.

The final pending change should:

    be clearly documented
    not contain credentials
    not break the application
    keep applicable tests passing
    remain unstaged

Prefer a small meaningful code/test change rather than whitespace-only noise.

Do not create an unexplained dirty state.

Expected final fixture state:

    workflow state = unstaged_only

unless the exact current M006 semantics classify the intentionally pending files differently.

Record literal:

    git status --short

for the Vocab App fixture in the closeout.

Do NOT clean this intentional state before closeout.

---

# Gate N — Populate Every M009 Browser Area

Start M009 against the Vocab App fixture:

    repoctl web \
        --repository /home/chuck/ai-agent-tests/vocab-app \
        --host 127.0.0.1 \
        --port 8765

Verify that the following pages now contain meaningful data rather than empty-state placeholders:

    Dashboard
    Context
    Snapshots
    Comparisons
    AI Review
    Workflow

Minimum evidence expectation:

Dashboard:

- real branch/HEAD;
- non-clean workflow state;
- staged/unstaged/untracked counts;
- snapshot candidate/matching information.

Context:

- run at least one meaningful query;
- real files/symbols/relationships/tests displayed.

Snapshots:

- multiple known snapshots visible.

Comparisons:

- multiple meaningful comparison records or at least two materially different comparisons;
- non-zero structural counts/details visible.

AI Review:

- at least one real live GPT-OSS analysis visible;
- grounded evidence IDs;
- advisory label.

Workflow:

- at least one M008 stage plan/execution;
- at least one M007 commit plan/execution;
- preferably multiple historical transactions after Gate L.

Browser must remain unable to mutate Git.

---

# Gate O — Desktop Width / Layout Improvement

The Product Owner observed that the M009 interface uses only roughly half of the available desktop browser width.

Improve the application shell to make materially better use of a normal desktop display.

Target direction:

    small responsive side margins
    approximately 1200–1400px maximum working width on large screens
    or equivalent responsive width around 90–95vw

Exact CSS values are implementation-specific.

Do not simply make every text paragraph extremely wide.

Use the additional space intelligently for:

    tables
    repository/status cards
    comparison summaries
    workflow history
    long repository paths
    evidence lists

Retain reasonable readable widths for prose-heavy text.

Responsive behavior should remain usable on narrower windows.

Do not introduce a frontend framework or external CSS dependency.

This is a presentation refinement to the existing local CSS.

---

# Gate P — Presentation-Only Refinement

After populating Vocab App evidence, inspect the actual rendered pages.

Permit small browser presentation improvements if real populated data exposes obvious issues such as:

    table clipping
    poor wrapping
    unusable path presentation
    excessively narrow columns
    evidence IDs overwhelming primary content
    weak visual separation between deterministic and advisory content
    poor empty-vs-populated hierarchy

Keep these changes presentation-focused.

Do NOT redesign core repository semantics.

Do NOT rename important deterministic concepts merely to make them friendlier without Product Owner review.

In particular, concepts such as:

    snapshot
    matching snapshot
    workflow state
    comparison
    evidence ID

should remain semantically stable during M009.1.

The Product Owner will decide after hands-on review whether terminology should change.

---

# Gate Q — Product Owner Review Environment

At technical closeout, provide exact commands for the Product Owner to launch the populated fixture UI.

Linux server command should be equivalent to:

    /home/chuck/.venvs/repo-control/bin/repoctl web \
        --repository /home/chuck/ai-agent-tests/vocab-app \
        --host 127.0.0.1 \
        --port 8765

Windows tunnel:

    ssh -N -L 8765:127.0.0.1:8765 chuck@192.168.1.173

Browser:

    http://127.0.0.1:8765

Do not hard-code environment-specific IPs into application logic.

These values are documentation for the current development environment only.

---

# Product Owner Usability Review Boundary

The coder is responsible for creating a populated, technically validated review environment.

The coder does NOT decide final UI terminology or information density.

After technical completion, the Product Owner will personally review:

    Dashboard
    Context
    Snapshots
    Comparisons
    AI Review
    Workflow

and provide usability feedback.

That feedback will control M010 UI composition.

Do not preemptively add browser Git mutation controls in M009.1.

---

# Repo Control Plane Code Scope

Expected Repo Control Plane implementation changes should be small.

Likely areas:

    src/repoctl/web/static/app.css

and, only where populated evidence demonstrates a real presentation issue:

    templates/
    views.py
    focused web tests
    README documentation if appropriate

Do NOT modify:

    scanner semantics
    relationship semantics
    context selection semantics
    snapshot identity
    comparison semantics
    GPT-OSS provider contract
    M007 commit safety
    M008 staging safety

unless an actual blocker is discovered.

If fixture population exposes a core defect:

    STOP — ESCALATION REQUIRED

Do not hide the defect with browser-only logic.

---

# Test / Validation Requirements — Repo Control Plane

Run focused M009/M009.1 web tests after presentation changes.

Then run the full Repo Control Plane suite:

    PYTHONPATH=src python3 -m unittest -q

or the established permanent-environment equivalent.

Use ordinary Python bytecode behavior.

Confirm M006.1 hygiene remains effective.

After regression, explicitly verify development-repository Git-operation markers are absent.

At minimum inspect:

    MERGE_HEAD
    rebase-merge
    rebase-apply
    REBASE_HEAD
    CHERRY_PICK_HEAD
    REVERT_HEAD
    BISECT_LOG
    BISECT_START

If any synthetic operation marker appears:

    STOP AND REPORT

Do not remove it automatically.

This is a hard requirement because prior milestones exposed test-state contamination.

---

# Vocab App Validation

Run the Vocab App's applicable existing test suite at meaningful healthy-state points.

Record:

    command
    pass/fail result
    test count where available

Temporary intentionally broken states may fail tests if deliberately created for evidence, but the final intentionally pending state must not be broken.

The final fixture should be suitable for M010 testing.

---

# No Remote Mutation

For the Vocab App fixture:

    no push
    no force push
    no remote branch creation required

For Repo Control Plane development:

    no commit or push unless separately instructed

M009.1 may create local Vocab App commits because those are explicitly part of the fixture history.

---

# External Evidence Preservation

Do not delete Repo Control Plane external evidence merely to make the UI cleaner.

Preserve generated:

    contexts
    snapshots
    comparisons
    analyses
    stage plans
    stage executions
    commit plans
    commit executions

for the Vocab App fixture.

These artifacts are the test data needed for Product Owner review.

Do not manually fabricate JSON evidence.

Generate evidence through existing Repo Control Plane services.

---

# Expected Fixture Evidence Arc

Conceptually the Vocab App fixture should finish with evidence resembling:

    baseline commit
        │
        └── snapshot A
                │
                ▼
        Change Set 1
                │
                ├── snapshot B
                ├── comparison A→B
                ├── GPT-OSS analysis
                ├── stage plan/execution
                ├── staged snapshot
                └── commit plan/execution
                        │
                        ▼
                   demo commit 1
                        │
                        ▼
        Change Set 2
                │
                ├── snapshot C
                ├── comparison
                ├── GPT-OSS analysis
                └── controlled correction
                        │
                        ├── stage plan/execution
                        ├── snapshot
                        └── commit plan/execution
                                │
                                ▼
                           demo commit 2
                                │
                                ▼
                    final harmless pending change
                                │
                                ▼
                         unstaged_only

The exact number of snapshots/analyses may vary if deterministic reuse occurs.

Do not generate meaningless artificial duplicates solely to increase counts.

---

# Browser Safety Revalidation

While running against populated Vocab App evidence, reconfirm that the browser itself does NOT:

    stage
    commit
    push
    fetch
    pull

The existence of M007/M008 workflow evidence must not accidentally expose mutation routes.

M010 remains the milestone that will deliberately add browser approval actions.

---

# Explicit Non-Goals

Do not implement:

- browser stage execution;
- browser commit execution;
- browser push;
- remote workflow;
- LAN-native unauthenticated exposure;
- authentication;
- multi-repository selection;
- source editor;
- terminal/shell access;
- GitHub integration;
- selective staging;
- partial-hunk staging;
- new AI model behavior;
- semantic search;
- test-result ingestion feature;
- broad UI redesign;
- production deployment;
- Photo Organizer access.

---

# Escalation Protocol

STOP and report if:

- Repo Control Plane development repository is not clean at start;
- Vocab App fixture is unexpectedly dirty at start;
- sensitive credential files are present;
- existing fixture branch state is ambiguous;
- Vocab App cannot be safely isolated on a dedicated branch;
- existing Repo Control Plane services fail against the real Vocab App fixture;
- generated workflow evidence does not appear in the browser;
- the browser requires semantic duplication to display it;
- M007/M008 behavior regresses;
- live GPT-OSS fails in a way inconsistent with M005.1;
- full tests recreate synthetic Git-operation markers;
- final Vocab App pending state is broken;
- any action would require Photo Organizer access.

Report:

1. exact gate;
2. observed state;
3. expected state;
4. Repo Control Plane mutations performed;
5. Vocab App mutations performed;
6. current branch/HEAD/status of both repositories;
7. evidence artifacts already created;
8. rollback performed or not;
9. smallest next decision required.

Do not automatically reset or destroy evidence.

---

# Required Closeout

Create:

`docs/009.1_browser_ui_usability_and_vocab_app_fixture_closeout.md`

Include:

1. status — PASS or ESCALATION;
2. Repo Control Plane initial branch/HEAD/clean preflight;
3. Vocab App original branch/HEAD/clean preflight;
4. credential-boundary result;
5. dedicated fixture branch created/used;
6. immutable Vocab App baseline SHA;
7. Repo Control Plane files changed;
8. desktop-width/layout changes;
9. any presentation refinements made;
10. baseline snapshot ID;
11. context queries/results used;
12. Change Set 1 description;
13. Change Set 1 test result;
14. snapshot/comparison/analysis IDs for Change Set 1;
15. first M008 stage plan/execution IDs;
16. staged snapshot ID;
17. first M007 commit plan/execution IDs and commit SHA;
18. Change Set 2 description;
19. deliberately broken/questionable-state result if used;
20. Change Set 2 snapshot/comparison/analysis IDs;
21. forward correction description;
22. second stage/commit workflow evidence IDs where created;
23. healthy-state Vocab App test result;
24. final intentionally pending change;
25. literal final Vocab App `git status --short`;
26. final Vocab App workflow state;
27. populated Dashboard evidence;
28. populated Context evidence;
29. populated Snapshot evidence;
30. populated Comparison evidence;
31. populated AI Review evidence;
32. populated Workflow evidence;
33. confirmation browser Git mutation remains unavailable;
34. focused web test result;
35. full Repo Control Plane regression result;
36. post-regression Git-operation-marker result;
37. Python-cache hygiene result;
38. no network Git mutation confirmation;
39. no Vocab App push confirmation;
40. no Photo Organizer access confirmation;
41. exact Linux web startup command for Product Owner;
42. exact Windows SSH tunnel/browser instructions;
43. limitations/deferred items;
44. recommendation for M010;
45. literal final Repo Control Plane `git status --short`.

Do not commit or push Repo Control Plane unless separately instructed.

---

# Acceptance Criteria

Milestone 009.1 passes technically only if:

- Repo Control Plane development begins clean;
- Vocab App begins from a known clean baseline;
- no credential-bearing fixture files are exposed;
- Vocab App work occurs only on a dedicated local test branch;
- original Vocab App branch/history remains preserved;
- baseline SHA is recorded;
- meaningful deterministic context is generated;
- multiple meaningful snapshots exist;
- at least one non-zero structural comparison exists;
- preferably multiple meaningfully different comparisons exist;
- live GPT-OSS analysis exists for meaningful comparison evidence;
- M008 stage plan/execution evidence exists for Vocab App;
- M007 commit plan/execution evidence exists for Vocab App;
- at least one controlled local Vocab App demo commit is created through Repo Control Plane;
- no Vocab App push occurs;
- any deliberately broken state is corrected forward;
- final Vocab App tests pass;
- final Vocab App state contains one intentional harmless unstaged change for M010;
- all six M009 browser pages contain meaningful data;
- browser remains unable to mutate Git;
- desktop application shell materially uses more screen width;
- populated tables/evidence remain readable;
- core Repo Control Plane semantics are not duplicated in the web layer;
- focused web tests pass;
- full Repo Control Plane regression passes;
- Python cache hygiene remains effective;
- no synthetic Git-operation markers remain after tests;
- Repo Control Plane development repository contains only intended M009.1 changes;
- no Photo Organizer access occurs.

Successful technical completion establishes a populated, realistic browser evaluation environment.

The Product Owner will then perform a hands-on UI review against the Vocab App fixture before M010 is composed.

M010 should use that Product Owner feedback when exposing the already-trusted M008/M007 workflow:

    review stage plan
        ↓
    Approve Stage
        ↓
    verified staged state
        ↓
    snapshot
        ↓
    review commit plan
        ↓
    Approve Commit
        ↓
    verified local commit
